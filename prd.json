{
  "project": "Mines Protocol \u2014 OPNet Port",
  "description": "Token-wrapping yield protocol on Bitcoin L1. Wrap OP_20 tokens into xTokens with fee-based yield. Staking rewards from protocol fees.",
  "stories": [
    {
      "id": "S01",
      "title": "Create contracts package.json",
      "description": "Create mines-opnet/contracts/package.json with exact dependencies from monorepo reference at ../monorepo/contracts/package.json. Must include: @btc-vision/bitcoin@7.0.0-alpha.11, @btc-vision/btc-runtime@^1.11.0-rc.10, @btc-vision/transaction@1.8.0-beta.10, opnet@1.8.1-beta.13, assemblyscript@0.27.36, @btc-vision/opnet-transform@1.2.0, dotenv. Add build scripts for 4 targets: build:miner, build:mine, build:factory, build:staking. Add deploy scripts for each.",
      "priority": 1,
      "passes": true,
      "acceptance": "File exists at mines-opnet/contracts/package.json. npm install completes without errors.",
      "model": "opus"
    },
    {
      "id": "S02",
      "title": "Create contracts asconfig.json",
      "description": "Create mines-opnet/contracts/asconfig.json with 4 targets: miner (outFile build/MinerToken.wasm, use abort=src/miner-token/index/abort), mine (outFile build/Mine.wasm, use abort=src/mine/index/abort), factory (outFile build/Factory.wasm, use abort=src/factory/index/abort), staking (outFile build/Staking.wasm, use abort=src/staking/index/abort). Copy options block exactly from ../monorepo/contracts/asconfig.json (sourceMap false, optimizeLevel 3, shrinkLevel 1, converge true, enable features, runtime stub, transform @btc-vision/opnet-transform).",
      "priority": 1,
      "passes": true,
      "acceptance": "File exists and is valid JSON. Options match monorepo reference exactly.",
      "model": "opus"
    },
    {
      "id": "S03",
      "title": "Create contracts tsconfig.json and .env.example",
      "description": "Create mines-opnet/contracts/tsconfig.json for AssemblyScript compatibility. Create .env.example with EC_PRIVATE_KEY and MLDSA_PRIVATE_KEY placeholders. Create empty build/ directory.",
      "priority": 1,
      "passes": true,
      "acceptance": "Both files exist. tsconfig.json is valid JSON.",
      "model": "opus"
    },
    {
      "id": "S04",
      "title": "Create MinerToken contract class",
      "description": "Create mines-opnet/contracts/src/miner-token/MinerToken.ts. Class MinerToken extends OP20. Import from @btc-vision/btc-runtime/runtime: Blockchain, BytesWriter, Calldata, OP20, OP20InitParameters. Import u256 from @btc-vision/as-bignum/assembly. Mark class @final. Override onDeployment: call super.onDeployment first, then this.instantiate with maxSupply=u256.fromString('1000000000000000000000000000') (1B with 18 decimals), decimals=18, name='Miner', symbol='MINER'. Pre-mint 10M MINER to Blockchain.tx.origin using this._mint.",
      "priority": 2,
      "passes": true,
      "acceptance": "File exists with correct class extending OP20, onDeployment calls super first, instantiate with correct params, pre-mint 10M to deployer.",
      "model": "opus"
    },
    {
      "id": "S05",
      "title": "Add MinerToken mine() free mint method",
      "description": "Add a public mine() method to MinerToken.ts. Decorated with @method() and @returns({ name: 'amount', type: ABIDataTypes.UINT256 }). Mints 1000 MINER (u256.fromString('1000000000000000000000')) to Blockchain.tx.sender. Returns the minted amount as BytesWriter(32) with writeU256. No payment required (testnet convenience).",
      "priority": 2,
      "passes": true,
      "acceptance": "mine() method exists, mints 1000 MINER to sender, returns amount in BytesWriter.",
      "model": "opus"
    },
    {
      "id": "S06",
      "title": "Create MinerToken index.ts boilerplate",
      "description": "Create mines-opnet/contracts/src/miner-token/index.ts following exact pattern from ../monorepo/contracts/src/alpha/index.ts. Import Blockchain from runtime, revertOnError from runtime/abort/abort, MinerToken from ./MinerToken. Set Blockchain.contract factory. Export runtime exports. Export abort function.",
      "priority": 2,
      "passes": true,
      "acceptance": "File exists with correct imports, contract factory, exports, and abort function matching the reference pattern.",
      "model": "opus"
    },
    {
      "id": "S07",
      "title": "Build MinerToken contract",
      "description": "Run npm run build:miner in the contracts directory. Fix any compilation errors. The command should be: asc src/miner-token/index.ts --target miner --measure --uncheckedBehavior never",
      "priority": 2,
      "passes": true,
      "acceptance": "build/MinerToken.wasm exists and is non-empty. Build completes without errors.",
      "model": "opus"
    },
    {
      "id": "S08",
      "title": "Create MinerToken deploy script",
      "description": "Create mines-opnet/contracts/scripts/deploy-miner.ts following exact pattern from ../monorepo/contracts/scripts/deploy-vault.ts. Change WASM path to build/MinerToken.wasm. Change console log title to 'MinerToken Deployment'. Keep all wallet loading, UTXO fetching, challenge fetching, signing, and broadcasting logic identical.",
      "priority": 3,
      "passes": true,
      "acceptance": "File exists. Script can be parsed by Node without syntax errors (node --check or similar).",
      "model": "opus"
    },
    {
      "id": "S09",
      "title": "Create Mine contract class skeleton",
      "description": "Create mines-opnet/contracts/src/mine/Mine.ts. Class Mine extends OP20. Imports: Blockchain, BytesWriter, BytesReader, Calldata, OP20, OP20InitParameters, Revert, SafeMath, TransferHelper, Address from runtime. Import u256. Mark @final. Define constants: MAX_DEPOSIT_WITHDRAW_FEE = u256.fromU32(200), MAX_CONTROLLER_PROTOCOL_FEE = u256.fromU32(100), FEE_DENOMINATOR = u256.fromU32(1000), ZERO = u256.fromU32(0).",
      "priority": 3,
      "passes": true,
      "acceptance": "File exists with class Mine extending OP20, all imports present, all constants defined.",
      "model": "opus"
    },
    {
      "id": "S10",
      "title": "Add Mine storage pointers",
      "description": "Add storage pointer fields to Mine class. All use Blockchain.nextPointer. Fields: _underlying (u16, stores Address of wrapped token), _owner (u16, stores controller Address), _factoryAddr (u16, stores factory/deployer Address), _wrapFee (u16, basis points out of 1000), _unwrapFee (u16), _controllerFee (u16), _protocolFee (u16), _controllerFeeAccrued (u16, accumulated controller fees), _protocolFeeAccrued (u16, accumulated protocol fees), _underlyingHeld (u16, self-tracked underlying balance).",
      "priority": 3,
      "passes": true,
      "acceptance": "Mine class has 10 private readonly u16 fields, each assigned Blockchain.nextPointer.",
      "model": "opus"
    },
    {
      "id": "S11",
      "title": "Add Mine storage helper methods",
      "description": "Add storage helper methods to Mine class matching MultSigVault pattern. fieldKeySimple(ptr: u16): Uint8Array \u2014 creates 32-byte key with ptr in first 2 bytes. su(key, val): void \u2014 writes u256 to storage. lu(key): u256 \u2014 reads u256 from storage. sa(key, addr): void \u2014 writes Address to storage via BytesWriter. la(key): Address \u2014 reads Address from storage via BytesReader. All marked @inline.",
      "priority": 3,
      "passes": true,
      "acceptance": "All 5 helper methods exist and match MultSigVault patterns exactly.",
      "model": "opus"
    },
    {
      "id": "S12",
      "title": "Add Mine onDeployment",
      "description": "Override onDeployment in Mine class. Call super.onDeployment(_calldata) FIRST. Read from calldata in order: underlying (Address via readAddress), decimals (u8 via readU8), name (string via readStringWithLength), symbol (string via readStringWithLength), wrapFee (u256 via readU256), unwrapFee (u256 via readU256), controllerFee (u256 via readU256), protocolFee (u256 via readU256). Set maxSupply to max u256 value. Call this.instantiate(new OP20InitParameters(maxSupply, decimals, name, symbol)). Store all values using sa/su with fieldKeySimple. Store _owner and _factoryAddr as Blockchain.tx.sender.",
      "priority": 4,
      "passes": true,
      "acceptance": "onDeployment reads 8 params from calldata, calls super first, calls instantiate, stores all values.",
      "model": "opus"
    },
    {
      "id": "S13",
      "title": "Add Mine access control helpers",
      "description": "Add private helper methods to Mine: requireOwner() \u2014 checks Blockchain.tx.sender == stored _owner, throws Revert('not owner'). requireFactoryOwner() \u2014 checks sender == stored _factoryAddr, throws Revert('not factory owner'). requireOwnerOrFactory() \u2014 checks sender is either _owner or _factoryAddr, throws Revert('not authorized').",
      "priority": 4,
      "passes": true,
      "acceptance": "Three access control methods exist, each reads stored address and compares with tx.sender.",
      "model": "opus"
    },
    {
      "id": "S14",
      "title": "Add Mine _underlyingBalance internal method",
      "description": "Add private _underlyingBalance(): u256 method to Mine. Returns: SafeMath.sub(SafeMath.sub(underlyingHeld, controllerFeeAccrued), protocolFeeAccrued). Reads all three from storage using lu(fieldKeySimple(ptr)). This is the net underlying available to xToken holders.",
      "priority": 4,
      "passes": true,
      "acceptance": "Method returns _underlyingHeld minus both fee accumulators, all via SafeMath.",
      "model": "opus"
    },
    {
      "id": "S15",
      "title": "Add Mine _getUnderlyingAmount internal method",
      "description": "Add private _getUnderlyingAmount(xAmount: u256): u256 method to Mine. If totalSupply() == ZERO, return xAmount (1:1 ratio). Otherwise return SafeMath.div(SafeMath.mul(xAmount, this._underlyingBalance()), this.totalSupply()). Uses inherited totalSupply() from OP20.",
      "priority": 4,
      "passes": true,
      "acceptance": "Method handles zero-supply edge case (returns input), otherwise calculates proportional underlying amount.",
      "model": "opus"
    },
    {
      "id": "S16",
      "title": "Add Mine _calcWrap fee algorithm",
      "description": "Add private _calcWrap(amount: u256) method to Mine. Port EXACTLY from Mine.sol lines 479-502. If totalSupply == 0: return (amount, ZERO, ZERO, ZERO). Otherwise: feeAmount = SafeMath.div(SafeMath.mul(amount, wrapFee), FEE_DENOMINATOR). controllerFeeAmount = SafeMath.div(SafeMath.mul(feeAmount, controllerFee), FEE_DENOMINATOR). protocolFeeAmount = SafeMath.div(SafeMath.mul(feeAmount, protocolFee), FEE_DENOMINATOR). stakersFee = SafeMath.sub(SafeMath.sub(feeAmount, controllerFeeAmount), protocolFeeAmount). xAmount = SafeMath.div(SafeMath.mul(totalSupply, SafeMath.sub(amount, feeAmount)), SafeMath.add(this._underlyingBalance(), stakersFee)). Since AssemblyScript has no tuple returns, store results in local variables and return them via a helper approach (4 separate method calls, or store in temporary storage, or use a WrapResult class).",
      "priority": 5,
      "passes": true,
      "acceptance": "Fee calculation matches Mine.sol exactly. All arithmetic uses SafeMath. Handles zero-supply case.",
      "model": "opus"
    },
    {
      "id": "S17",
      "title": "Add Mine wrap() public method",
      "description": "Add public wrap(calldata: Calldata): BytesWriter method to Mine. Decorated with @method() and @returns. Reads amount (u256) from calldata. CHECKS: amount must be > ZERO. EFFECTS: Calculate fees via _calcWrap. Update _controllerFeeAccrued and _protocolFeeAccrued by adding their portions. INTERACTIONS: TransferHelper.transferFrom(underlying, Blockchain.tx.sender, Blockchain.contractAddress, amount). Update _underlyingHeld by adding amount. Mint xTokens to sender: this._mint(Blockchain.tx.sender, xAmount). Return xAmount in BytesWriter(32).",
      "priority": 5,
      "passes": true,
      "acceptance": "wrap() reads amount, validates, calculates fees, updates accumulators, transfers underlying, increments _underlyingHeld, mints xTokens, returns xAmount. CEI pattern followed.",
      "model": "opus"
    },
    {
      "id": "S18",
      "title": "Add Mine unwrap() public method",
      "description": "Add public unwrap(calldata: Calldata): BytesWriter method to Mine. Reads xAmount (u256). CHECKS: xAmount > ZERO. Calculate underlyingAmount via _getUnderlyingAmount(xAmount). Calculate feeAmount = SafeMath.div(SafeMath.mul(underlyingAmount, unwrapFee), FEE_DENOMINATOR). Calculate controller/protocol portions of fee. EFFECTS: this._burn(Blockchain.tx.sender, xAmount). Update _controllerFeeAccrued, _protocolFeeAccrued. Calculate netUnderlying = SafeMath.sub(underlyingAmount, feeAmount). Decrement _underlyingHeld by netUnderlying. INTERACTIONS: TransferHelper.transfer(underlying, Blockchain.tx.sender, netUnderlying). Return netUnderlying.",
      "priority": 5,
      "passes": true,
      "acceptance": "unwrap() burns xTokens, calculates unwrap fee, updates accumulators, decrements _underlyingHeld, transfers net underlying to sender. CEI pattern followed.",
      "model": "opus"
    },
    {
      "id": "S19",
      "title": "Create Mine index.ts boilerplate",
      "description": "Create mines-opnet/contracts/src/mine/index.ts following exact pattern from MinerToken index.ts but importing Mine class instead. Blockchain.contract factory returns new Mine(). Export runtime exports. Export abort function with abort=src/mine/index/abort.",
      "priority": 5,
      "passes": true,
      "acceptance": "File exists with correct imports and factory function returning Mine instance.",
      "model": "opus"
    },
    {
      "id": "S20",
      "title": "Build Mine contract (wrap/unwrap)",
      "description": "Run npm run build:mine. Fix any compilation errors. Ensure all SafeMath usage is correct, all imports resolve, no while loops exist.",
      "priority": 5,
      "passes": true,
      "acceptance": "build/Mine.wasm exists and is non-empty. Build completes without errors.",
      "model": "opus"
    },
    {
      "id": "S21",
      "title": "Add Mine getWrappedAmount view",
      "description": "Add public getWrappedAmount(calldata: Calldata): BytesWriter to Mine. @method() @returns UINT256. Reads amount (u256) from calldata. Calls _calcWrap(amount) and returns only the xAmount portion in BytesWriter(32). This is a read-only preview of how many xTokens the user would receive.",
      "priority": 6,
      "passes": true,
      "acceptance": "View method exists, calls _calcWrap, returns xAmount only.",
      "model": "opus"
    },
    {
      "id": "S22",
      "title": "Add Mine getUnwrappedAmount view",
      "description": "Add public getUnwrappedAmount(calldata: Calldata): BytesWriter to Mine. Reads xAmount. Calculates underlyingAmount = _getUnderlyingAmount(xAmount). Calculates feeAmount = underlyingAmount * unwrapFee / 1000 (SafeMath). Returns SafeMath.sub(underlyingAmount, feeAmount) in BytesWriter(32).",
      "priority": 6,
      "passes": true,
      "acceptance": "View method returns net underlying after unwrap fee deduction.",
      "model": "opus"
    },
    {
      "id": "S23",
      "title": "Add Mine underlyingBalance and getUnderlyingAmount public views",
      "description": "Add two public view methods: underlyingBalance(calldata): returns this._underlyingBalance() in BytesWriter(32). getUnderlyingAmount(calldata): reads xAmount u256, returns this._getUnderlyingAmount(xAmount) in BytesWriter(32). Both @method() @returns UINT256.",
      "priority": 6,
      "passes": true,
      "acceptance": "Both view methods exist and return correct u256 values.",
      "model": "opus"
    },
    {
      "id": "S24",
      "title": "Add Mine fee getter views",
      "description": "Add 6 public view methods to Mine, each @method() @returns UINT256: getWrapFee \u2014 returns stored _wrapFee. getUnwrapFee \u2014 returns stored _unwrapFee. getControllerFee \u2014 returns stored _controllerFee. getProtocolFee \u2014 returns stored _protocolFee. getProtocolFeeAccrued \u2014 returns stored _protocolFeeAccrued. getControllerFeeAccrued \u2014 returns stored _controllerFeeAccrued. Each reads from storage via lu(fieldKeySimple(ptr)) and writes to BytesWriter(32).",
      "priority": 6,
      "passes": true,
      "acceptance": "All 6 fee getter methods exist and return correct stored values.",
      "model": "opus"
    },
    {
      "id": "S25",
      "title": "Add Mine underlying() and getOwner() views",
      "description": "Add two public view methods: getUnderlying(calldata): reads and returns _underlying address via la(). getOwner(calldata): reads and returns _owner address via la(). Both write Address to BytesWriter using writeAddress.",
      "priority": 6,
      "passes": true,
      "acceptance": "Both methods return stored addresses correctly.",
      "model": "opus"
    },
    {
      "id": "S26",
      "title": "Add Mine fee setter methods",
      "description": "Add 4 public methods to Mine: setWrapFee(calldata) \u2014 reads fee u256, requireOwnerOrFactory(), validate fee <= MAX_DEPOSIT_WITHDRAW_FEE, store. setUnwrapFee(calldata) \u2014 same pattern. setControllerFee(calldata) \u2014 reads fee, requireOwnerOrFactory(), validate <= MAX_CONTROLLER_PROTOCOL_FEE, store. setProtocolFee(calldata) \u2014 reads fee, requireFactoryOwner() only, validate <= MAX_CONTROLLER_PROTOCOL_FEE, store. All return boolean true in BytesWriter(1).",
      "priority": 7,
      "passes": true,
      "acceptance": "All 4 setters exist with proper access control and validation. setProtocolFee restricted to factory owner only.",
      "model": "opus"
    },
    {
      "id": "S27",
      "title": "Add Mine claimControllerFee method",
      "description": "Add public claimControllerFee(calldata): BytesWriter. requireOwner(). Read _controllerFeeAccrued. If ZERO, throw Revert('no fee'). Reset _controllerFeeAccrued to ZERO. Read _underlying address. Decrement _underlyingHeld by accrued amount. TransferHelper.transfer(underlying, Blockchain.tx.sender, accrued). Return accrued in BytesWriter(32).",
      "priority": 7,
      "passes": true,
      "acceptance": "Method transfers accrued controller fees to owner, resets accumulator, decrements _underlyingHeld.",
      "model": "opus"
    },
    {
      "id": "S28",
      "title": "Add Mine claimProtocolFee method",
      "description": "Add public claimProtocolFee(calldata): BytesWriter. requireFactoryOwner(). Read _protocolFeeAccrued. If ZERO, throw Revert('no fee'). Reset _protocolFeeAccrued to ZERO. Decrement _underlyingHeld by accrued amount. TransferHelper.transfer(underlying, Blockchain.tx.sender, accrued). Return accrued in BytesWriter(32).",
      "priority": 7,
      "passes": true,
      "acceptance": "Method transfers accrued protocol fees to factory owner, resets accumulator, decrements _underlyingHeld.",
      "model": "opus"
    },
    {
      "id": "S29",
      "title": "Add Mine disburseProtocolFee method",
      "description": "Add public disburseProtocolFee(calldata): BytesWriter. Read recipient Address from calldata. Read _protocolFeeAccrued. If ZERO, return ZERO (no revert \u2014 staking calls this speculatively). Reset _protocolFeeAccrued to ZERO. Decrement _underlyingHeld by accrued amount. TransferHelper.transfer(underlying, recipient, accrued). Return accrued amount. This method is called by the Staking contract to pull protocol fees for reward distribution.",
      "priority": 7,
      "passes": true,
      "acceptance": "Method transfers protocol fees to specified recipient (Staking contract), returns amount disbursed. Returns ZERO gracefully if nothing accrued.",
      "model": "opus"
    },
    {
      "id": "S30",
      "title": "Build complete Mine contract",
      "description": "Run npm run build:mine. Fix any compilation errors from all the added methods (S09-S29). Verify all SafeMath usage, no while loops, all storage pointers unique.",
      "priority": 7,
      "passes": true,
      "acceptance": "build/Mine.wasm exists and is non-empty. No compilation errors.",
      "model": "opus"
    },
    {
      "id": "S31",
      "title": "Create Mine deploy script",
      "description": "Create mines-opnet/contracts/scripts/deploy-mine.ts. Follow deploy-miner.ts pattern but load build/Mine.wasm. Title: 'Mine Deployment'. Note: Mine deployment requires calldata params (underlying address, decimals, name, symbol, fees) \u2014 add these as constants or CLI args at the top of the script. Use BinaryWriter to encode deployment calldata.",
      "priority": 8,
      "passes": true,
      "acceptance": "File exists. Includes calldata encoding for Mine deployment parameters.",
      "model": "opus"
    },
    {
      "id": "S32",
      "title": "Create Factory contract class skeleton",
      "description": "Create mines-opnet/contracts/src/factory/Factory.ts. Class Factory extends OP_NET. Imports: Blockchain, BytesWriter, BytesReader, Calldata, OP_NET, Revert, SafeMath, StoredU256, Address. Import u256. Mark @final. Constants: ZERO, ONE, EMPTY_SUB = new Uint8Array(30).",
      "priority": 8,
      "passes": true,
      "acceptance": "File exists with Factory class extending OP_NET, all imports, constants.",
      "model": "opus"
    },
    {
      "id": "S33",
      "title": "Add Factory storage and helpers",
      "description": "Add to Factory: Storage: _owner (u16), _mineCount (StoredU256 with Blockchain.nextPointer and EMPTY_SUB), pMineByUnderlying (u16 for mapping underlying\u2192mine), pMineByIndex (u16 for mapping index\u2192mine), pUnderlyingByMine (u16 for reverse lookup mine\u2192underlying). Storage helpers: su, lu, sa, la, fieldKeySimple (same as Mine). Add addrKey(ptr, addr) for address-keyed lookups \u2014 key format: [ptr(2)] + [addr first 30 bytes]. Add idxKey(ptr, idx: u256) for index-keyed lookups \u2014 key format: [ptr(2)] + [zeros(26)] + [idx_lo32(4)].",
      "priority": 8,
      "passes": true,
      "acceptance": "Factory has 5 storage fields, all helper methods for different key patterns.",
      "model": "opus"
    },
    {
      "id": "S34",
      "title": "Add Factory onDeployment and requireOwner",
      "description": "Override onDeployment: call super.onDeployment first. Store Blockchain.tx.sender as _owner. Add private requireOwner() that checks sender == _owner, throws Revert('not owner').",
      "priority": 8,
      "passes": true,
      "acceptance": "onDeployment stores owner. requireOwner validates caller.",
      "model": "opus"
    },
    {
      "id": "S35",
      "title": "Add Factory registerMine method",
      "description": "Add public registerMine(calldata): BytesWriter. @method() @returns UINT256. requireOwner(). Read underlying (Address) and mineAddress (Address) from calldata. Store mapping: addrKey(pMineByUnderlying, underlying) \u2192 mineAddress. Store reverse: addrKey(pUnderlyingByMine, mineAddress) \u2192 underlying. Get current count from _mineCount. Store: idxKey(pMineByIndex, count) \u2192 mineAddress. Increment _mineCount. Return new count.",
      "priority": 9,
      "passes": true,
      "acceptance": "registerMine stores both mappings, increments count. Owner-only.",
      "model": "opus"
    },
    {
      "id": "S36",
      "title": "Add Factory view methods",
      "description": "Add 3 public view methods to Factory: getMineAddress(calldata) \u2014 reads underlying Address, returns mine Address from addrKey lookup. getMineCount(calldata) \u2014 returns _mineCount.value. getMineAtIndex(calldata) \u2014 reads index u256, validates < _mineCount.value, returns mine Address from idxKey lookup. All @method() with appropriate @returns.",
      "priority": 9,
      "passes": true,
      "acceptance": "All 3 views return correct data. getMineAtIndex validates bounds.",
      "model": "opus"
    },
    {
      "id": "S37",
      "title": "Create Factory index.ts and build",
      "description": "Create src/factory/index.ts boilerplate (same pattern). Run npm run build:factory. Fix compilation errors.",
      "priority": 9,
      "passes": true,
      "acceptance": "build/Factory.wasm exists and is non-empty.",
      "model": "opus"
    },
    {
      "id": "S38",
      "title": "Create Factory deploy script",
      "description": "Create scripts/deploy-factory.ts following deploy pattern. Load build/Factory.wasm. Title: 'Factory Deployment'. No special calldata needed (onDeployment only reads sender).",
      "priority": 9,
      "passes": true,
      "acceptance": "File exists. Script follows deploy pattern correctly.",
      "model": "opus"
    },
    {
      "id": "S39",
      "title": "Create Staking contract class skeleton",
      "description": "Create mines-opnet/contracts/src/staking/Staking.ts. Class Staking extends OP_NET. Same imports as Factory plus TransferHelper. Mark @final. Constants: ZERO, ONE, EMPTY_SUB, POINT_MULTIPLIER = u256.fromString('1000000000000000000').",
      "priority": 10,
      "passes": true,
      "acceptance": "File exists with Staking extending OP_NET, all imports, POINT_MULTIPLIER constant.",
      "model": "opus"
    },
    {
      "id": "S40",
      "title": "Add Staking storage pointers and key helpers",
      "description": "Add to Staking: _xMiner (u16, Address of MINER token), _owner (u16). Per-mine pointers: pMineSupply (u16), pMineTotalPoints (u16). Per-user-per-mine pointers: pRecordBalance (u16), pRecordReward (u16), pRecordLastPoints (u16). Add helpers: su, lu, sa, la, fieldKeySimple (same pattern). Add mineKey(ptr, mine: Address): Uint8Array \u2014 [ptr(2)] + [mine first 30 bytes]. Add userMineKey(ptr, mine: Address, user: Address): Uint8Array \u2014 [ptr(2)] + [mine first 15 bytes] + [user first 15 bytes] (compound key for 2 addresses).",
      "priority": 10,
      "passes": true,
      "acceptance": "Staking has 7 storage pointers, su/lu/sa/la helpers, mineKey and userMineKey compound key builders.",
      "model": "opus"
    },
    {
      "id": "S41",
      "title": "Add Staking onDeployment",
      "description": "Override onDeployment: call super.onDeployment first. Read xMiner Address from calldata. Store _xMiner and _owner (= tx.sender).",
      "priority": 10,
      "passes": true,
      "acceptance": "onDeployment reads xMiner address, stores both fields.",
      "model": "opus"
    },
    {
      "id": "S42",
      "title": "Add Staking _newRewards and _updateRewards internal methods",
      "description": "Add private _newRewards(mineAddr: Address, user: Address, totalPoints: u256): u256. Reads lastPoints from userMineKey(pRecordLastPoints, mine, user). Calculates points = SafeMath.sub(totalPoints, lastPoints). Reads balance from userMineKey(pRecordBalance, mine, user). Returns SafeMath.div(SafeMath.mul(points, balance), POINT_MULTIPLIER). Add private _updateRewards(mineAddr: Address, user: Address, totalPoints: u256): void. Calculates owing = _newRewards(...). If owing > ZERO: adds to stored reward. Updates lastPoints = totalPoints.",
      "priority": 11,
      "passes": true,
      "acceptance": "Both methods implement points-based reward calculation matching Staking.sol logic.",
      "model": "opus"
    },
    {
      "id": "S43",
      "title": "Add Staking _disburse internal method",
      "description": "Add private _disburse(mineAddr: Address): u256. This calls the Mine contract's disburseProtocolFee method to pull accrued protocol fees. Use TransferHelper or direct cross-contract call. Read current mineSupply from mineKey(pMineSupply, mine). If supply > ZERO and disbursed > ZERO: update totalPoints += SafeMath.div(SafeMath.mul(disbursed, POINT_MULTIPLIER), supply). Store updated totalPoints. Return disbursed amount.",
      "priority": 11,
      "passes": true,
      "acceptance": "_disburse pulls fees from Mine, updates totalPoints proportionally to staked supply.",
      "model": "opus"
    },
    {
      "id": "S44",
      "title": "Add Staking stake() method",
      "description": "Add public stake(calldata): BytesWriter. @method() @returns BOOL. Read mine Address and amount u256 from calldata. Validate amount > ZERO. Call _disburse(mine) to pull pending fees. Read current totalPoints. Call _updateRewards(mine, sender, totalPoints). TransferHelper.transferFrom(_xMiner, sender, contractAddress, amount). Increment user balance in userMineKey(pRecordBalance). Increment mineSupply in mineKey(pMineSupply). Return true.",
      "priority": 11,
      "passes": true,
      "acceptance": "stake() disburses, updates rewards, transfers MINER tokens in, increments balances.",
      "model": "opus"
    },
    {
      "id": "S45",
      "title": "Add Staking unstake() method",
      "description": "Add public unstake(calldata): BytesWriter. Read mine Address and amount u256. Validate amount > ZERO. _disburse(mine). Read totalPoints. _updateRewards. Read user balance, validate >= amount. Decrement user balance. Decrement mineSupply. TransferHelper.transfer(_xMiner, sender, amount). Return true.",
      "priority": 12,
      "passes": true,
      "acceptance": "unstake() validates balance, decrements, transfers MINER tokens back to user.",
      "model": "opus"
    },
    {
      "id": "S46",
      "title": "Add Staking claim() method",
      "description": "Add public claim(calldata): BytesWriter. @method() @returns UINT256. Read mine Address. _disburse(mine). Read totalPoints. _updateRewards(mine, sender, totalPoints). Read user reward from userMineKey(pRecordReward). If ZERO, throw Revert('no rewards'). Reset reward to ZERO. Get underlying token address by calling Mine contract or storing it. TransferHelper.transfer(underlying, sender, reward). Return reward amount.",
      "priority": 12,
      "passes": true,
      "acceptance": "claim() disburses, calculates owed rewards, transfers underlying tokens to user, resets reward.",
      "model": "opus"
    },
    {
      "id": "S47",
      "title": "Add Staking view methods",
      "description": "Add two view methods: getRewards(calldata) \u2014 reads mine Address and staker Address. Reads totalPoints from mineKey. Computes pending = stored reward + _newRewards(mine, staker, totalPoints). Returns pending u256. getStakeBalance(calldata) \u2014 reads mine Address and staker Address. Returns balance from userMineKey(pRecordBalance). Both @method() @returns UINT256.",
      "priority": 12,
      "passes": true,
      "acceptance": "Both views return correct pending rewards and staked balance.",
      "model": "opus"
    },
    {
      "id": "S48",
      "title": "Create Staking index.ts and build",
      "description": "Create src/staking/index.ts boilerplate. Run npm run build:staking. Fix compilation errors.",
      "priority": 12,
      "passes": true,
      "acceptance": "build/Staking.wasm exists and is non-empty.",
      "model": "opus"
    },
    {
      "id": "S49",
      "title": "Create Staking deploy script",
      "description": "Create scripts/deploy-staking.ts. Load build/Staking.wasm. Encode calldata with xMiner token address (BinaryWriter.writeAddress). Title: 'Staking Deployment'.",
      "priority": 13,
      "passes": true,
      "acceptance": "File exists with calldata encoding for xMiner address parameter.",
      "model": "opus"
    },
    {
      "id": "S50",
      "title": "Build all 4 contracts successfully",
      "description": "Run npm run build (all targets). Fix any remaining compilation errors across all contracts. Verify all 4 WASM files exist in build/.",
      "priority": 13,
      "passes": true,
      "acceptance": "build/MinerToken.wasm, build/Mine.wasm, build/Factory.wasm, build/Staking.wasm all exist and are non-empty.",
      "model": "opus"
    },
    {
      "id": "S51",
      "title": "Deploy MinerToken to testnet",
      "description": "Run npm run deploy:miner. Save the contract address. Update a deployed-addresses.json file with {minerToken: address}.",
      "priority": 14,
      "passes": true,
      "acceptance": "MinerToken deployed. Address saved in deployed-addresses.json.",
      "model": "opus"
    },
    {
      "id": "S52",
      "title": "Deploy Factory to testnet",
      "description": "Run npm run deploy:factory. Save address to deployed-addresses.json.",
      "priority": 14,
      "passes": true,
      "acceptance": "Factory deployed. Address saved.",
      "model": "opus"
    },
    {
      "id": "S53",
      "title": "Deploy Mine to testnet",
      "description": "Update deploy-mine.ts with MinerToken address as underlying. Set name='xMINER', symbol='xMINER', decimals=18, wrapFee=50 (5%), unwrapFee=50 (5%), controllerFee=100 (10%), protocolFee=100 (10%). Deploy. Save address.",
      "priority": 14,
      "passes": true,
      "acceptance": "Mine deployed with correct params. Address saved.",
      "model": "opus"
    },
    {
      "id": "S54",
      "title": "Register Mine in Factory and deploy Staking",
      "description": "Create a script or modify deploy script to call Factory.registerMine(minerTokenAddr, mineAddr). Deploy Staking with xMiner=MinerToken address. Save all addresses to deployed-addresses.json.",
      "priority": 14,
      "passes": true,
      "acceptance": "Mine registered in Factory. Staking deployed. deployed-addresses.json has all 4 addresses.",
      "model": "opus"
    },
    {
      "id": "S55",
      "title": "Scaffold React + Vite + TypeScript frontend",
      "description": "Create mines-opnet/frontend/ using npm create vite@latest -- --template react-ts. Install dependencies: @btc-vision/walletconnect@^1.9.12, opnet@1.8.1-beta.13, @btc-vision/transaction@1.8.0-beta.10, @btc-vision/bitcoin@7.0.0-alpha.11, react-router-dom, tailwindcss, @tailwindcss/vite. Configure vite.config.ts: base './', add tailwind plugin. Create src/styles/globals.css with Tailwind directives and dark theme base styles (bg-gray-950, text-gray-100).",
      "priority": 15,
      "passes": true,
      "acceptance": "npm run dev starts without errors. Page renders with dark background.",
      "model": "sonnet"
    },
    {
      "id": "S56",
      "title": "Create frontend config and provider",
      "description": "Create src/config.ts: export NETWORK (networks.testnet), RPC_URL ('https://testnet.opnet.org'), all 4 contract addresses from deployed-addresses.json. Create src/lib/provider.ts: export singleton JSONRpcProvider(RPC_URL, NETWORK). Import from 'opnet'.",
      "priority": 15,
      "passes": true,
      "acceptance": "Both files exist. Provider can be imported and used.",
      "model": "sonnet"
    },
    {
      "id": "S57",
      "title": "Create frontend helpers.ts",
      "description": "Create src/lib/helpers.ts with: formatBalance(raw: bigint, decimals: number): string \u2014 formats with 4 decimal places, no trailing zeros. truncateAddress(addr: string): string \u2014 shows first 8 + last 6 chars. parseAmount(input: string, decimals: number): bigint \u2014 parses user input to bigint with decimals. toHex(bytes: Uint8Array): string \u2014 converts to 0x-prefixed hex string.",
      "priority": 15,
      "passes": true,
      "acceptance": "All 4 utility functions exist and handle edge cases (zero, empty string, etc.).",
      "model": "sonnet"
    },
    {
      "id": "S58",
      "title": "Create contract ABIs",
      "description": "Create src/lib/contracts.ts. Define ABI arrays for all 4 contracts matching their @method/@returns signatures. Each ABI entry: { name, type: 'function', inputs: [...], outputs: [...] }. MinerToken ABI: mine(). Mine ABI: wrap, unwrap, getWrappedAmount, getUnwrappedAmount, underlyingBalance, getUnderlyingAmount, getWrapFee, getUnwrapFee, getControllerFee, getProtocolFee, getProtocolFeeAccrued, getControllerFeeAccrued, getUnderlying, getOwner, setWrapFee, setUnwrapFee, setControllerFee, setProtocolFee, claimControllerFee, claimProtocolFee, disburseProtocolFee. Factory ABI: registerMine, getMineAddress, getMineCount, getMineAtIndex. Staking ABI: stake, unstake, claim, getRewards, getStakeBalance. Also export OP_20_ABI from opnet for standard token methods.",
      "priority": 16,
      "passes": true,
      "acceptance": "All ABIs defined. Method names match contract implementations exactly.",
      "model": "sonnet"
    },
    {
      "id": "S59",
      "title": "Create useWallet hook",
      "description": "Create src/hooks/useWallet.ts. React hook using useState/useEffect. State: address (string|null), isConnected (boolean), identityKey (string|null), tweakedKey (string|null), senderAddress (Address|null). connect(): instantiate OPWalletConnect, get wallet address, decode bech32 to tweaked hex, resolve identity key via provider.getPublicKeyInfo(tweakedHex, false), build Address.fromString(identityHex, '0x02'+tweakedHex.slice(2)). disconnect(): clear state. Import Address from @btc-vision/transaction, NOT from @btc-vision/bitcoin.",
      "priority": 16,
      "passes": true,
      "acceptance": "Hook manages wallet state. connect() resolves identity key. disconnect() clears state.",
      "model": "sonnet"
    },
    {
      "id": "S60",
      "title": "Create WalletButton component",
      "description": "Create src/components/WalletButton.tsx. Uses useWallet hook. When disconnected: shows 'Connect Wallet' button with purple/blue accent. When connected: shows truncated address. Click toggles connect/disconnect. Style: rounded, dark theme consistent.",
      "priority": 16,
      "passes": true,
      "acceptance": "Component renders connect/disconnect states correctly.",
      "model": "sonnet"
    },
    {
      "id": "S61",
      "title": "Create Layout and Navbar components",
      "description": "Create src/components/Layout.tsx: flex column, min-h-screen, dark bg. Renders Navbar at top + children. Create src/components/Navbar.tsx: flex row, logo/title 'Mines Protocol', nav links (Home, Wrap, Unwrap, Stake), WalletButton on right. Responsive: hamburger menu on mobile. Use react-router-dom Link components.",
      "priority": 17,
      "passes": true,
      "acceptance": "Layout wraps pages. Navbar shows title, links, wallet button. Responsive on mobile.",
      "model": "sonnet"
    },
    {
      "id": "S62",
      "title": "Create App.tsx with HashRouter and routes",
      "description": "Create src/App.tsx. Use HashRouter (not BrowserRouter) for IPFS compatibility. Routes: / \u2192 HomePage, /mine/:address \u2192 MineDetailPage, /wrap \u2192 WrapPage, /wrap/:address \u2192 WrapPage, /unwrap \u2192 UnwrapPage, /unwrap/:address \u2192 UnwrapPage, /stake \u2192 StakingPage. Wrap all routes in Layout component. Update main.tsx to render App.",
      "priority": 17,
      "passes": true,
      "acceptance": "HashRouter with all routes. Navigation between pages works.",
      "model": "sonnet"
    },
    {
      "id": "S63",
      "title": "Fix WalletConnect popup CSS",
      "description": "Add global CSS to fix OPWallet connect popup positioning. In globals.css or a dedicated override: .walletconnect-modal__base { position: fixed !important; top: 50% !important; left: 50% !important; transform: translate(-50%, -50%) !important; z-index: 99999 !important; } Ensure popup appears centered and above all content.",
      "priority": 17,
      "passes": true,
      "acceptance": "WalletConnect popup appears centered as a modal overlay.",
      "model": "sonnet"
    },
    {
      "id": "S64",
      "title": "Create useMines hook",
      "description": "Create src/hooks/useMines.ts. Fetches all mines from Factory contract. On mount: call getMineCount(), then loop getMineAtIndex(i) for each index. For each mine address, fetch: name, symbol (from OP_20 interface), underlyingBalance, totalSupply (to compute ratio), getWrapFee, getUnwrapFee. Use getContract from opnet with provider (no sender needed for reads). Return: mines array, loading boolean, error.",
      "priority": 18,
      "passes": true,
      "acceptance": "Hook fetches mine list from Factory, enriches with on-chain data.",
      "model": "sonnet"
    },
    {
      "id": "S65",
      "title": "Create MineCard component",
      "description": "Create src/components/MineCard.tsx. Props: mine data (address, name, symbol, ratio, wrapFee, unwrapFee, underlyingBalance). Renders a card with: name/symbol header, ratio display (formatted to 4 decimals), fees (as percentages: fee/10 %), underlying balance. Card is clickable \u2014 links to /mine/:address. Dark theme card style (bg-gray-900, border-gray-800, hover effect).",
      "priority": 18,
      "passes": true,
      "acceptance": "Card renders mine info, links to detail page, dark theme styled.",
      "model": "sonnet"
    },
    {
      "id": "S66",
      "title": "Create HomePage",
      "description": "Create src/pages/HomePage.tsx. Uses useMines hook. Shows page title 'Mines'. Loading state: skeleton cards. Empty state: 'No mines found' message. Data state: grid of MineCards (responsive: 1 col mobile, 2 col tablet, 3 col desktop). Optional: link to create mine (future feature).",
      "priority": 18,
      "passes": true,
      "acceptance": "Page renders mine grid from on-chain data. Loading and empty states handled.",
      "model": "sonnet"
    },
    {
      "id": "S67",
      "title": "Create TokenInput component",
      "description": "Create src/components/TokenInput.tsx. Props: value, onChange, max (bigint), decimals, symbol, label, disabled. Renders: label, number input field, MAX button (sets value to max), token symbol badge. Styling: dark input (bg-gray-800, text-white, border-gray-700). MAX button highlights on hover.",
      "priority": 19,
      "passes": true,
      "acceptance": "Input handles numeric entry, MAX button sets maximum, displays token symbol.",
      "model": "sonnet"
    },
    {
      "id": "S68",
      "title": "Create TransactionButton component",
      "description": "Create src/components/TransactionButton.tsx. Props: onClick (async), label, disabled, loading. Renders: button with label, shows spinner when loading, disabled state styling. Handles async onClick with try/catch \u2014 shows error state briefly on failure. Dark theme: purple/blue gradient button.",
      "priority": 19,
      "passes": true,
      "acceptance": "Button handles async operations with loading spinner and error states.",
      "model": "sonnet"
    },
    {
      "id": "S69",
      "title": "Create useMine hook",
      "description": "Create src/hooks/useMine.ts. Takes mine address as param. Fetches: name, symbol, totalSupply, underlyingBalance (ratio = balance/supply), all fees, underlying token address, owner. If wallet connected: fetches user's xToken balance via OP_20 balanceOf, user's underlying token balance, user's underlying value (via getUnderlyingAmount). Returns all data + loading + error + refetch function.",
      "priority": 19,
      "passes": true,
      "acceptance": "Hook fetches comprehensive mine data including user-specific balances when wallet connected.",
      "model": "sonnet"
    },
    {
      "id": "S70",
      "title": "Create WrapPage",
      "description": "Create src/pages/WrapPage.tsx. If URL has :address param, use that mine. Otherwise show mine selector dropdown (from useMines). TokenInput for amount (underlying token). Preview section: calls getWrappedAmount to show estimated xTokens received, shows exchange rate and fee. Wrap button flow: (1) getContract for underlying token with sender, (2) check allowance, (3) if insufficient call increaseAllowance and sendTransaction(signer:null, mldsaSigner:null), (4) getContract for Mine with sender, (5) simulate wrap(amount), (6) sendTransaction. Show success message with new balance. Use useWallet for sender address.",
      "priority": 20,
      "passes": true,
      "acceptance": "Full wrap flow: select mine \u2192 enter amount \u2192 see preview \u2192 approve allowance \u2192 wrap \u2192 success. All transactions use null signers.",
      "model": "sonnet"
    },
    {
      "id": "S71",
      "title": "Create UnwrapPage",
      "description": "Create src/pages/UnwrapPage.tsx. Similar to WrapPage but reversed. TokenInput for xToken amount (user's xToken balance as max). Preview: calls getUnwrappedAmount to show estimated underlying received. Unwrap button: simulate unwrap(xAmount), sendTransaction(signer:null, mldsaSigner:null). No allowance step needed (burning own tokens). Show success with updated balances.",
      "priority": 20,
      "passes": true,
      "acceptance": "Full unwrap flow: select mine \u2192 enter xToken amount \u2192 see preview \u2192 unwrap \u2192 success.",
      "model": "sonnet"
    },
    {
      "id": "S72",
      "title": "Create MineDetailPage",
      "description": "Create src/pages/MineDetailPage.tsx. Route param: :address. Uses useMine hook. Displays: mine name/symbol as title, underlying token info, ratio (formatted), wrap fee %, unwrap fee %, controller fee %, protocol fee %, total underlying balance, total xToken supply. If wallet connected: user's xToken balance, user's underlying value. Action buttons: 'Wrap' (links to /wrap/:address), 'Unwrap' (links to /unwrap/:address). Dark themed stat cards layout.",
      "priority": 20,
      "passes": true,
      "acceptance": "Page displays all mine statistics. User balances shown when wallet connected. Action buttons link correctly.",
      "model": "sonnet"
    },
    {
      "id": "S73",
      "title": "Create useStaking hook",
      "description": "Create src/hooks/useStaking.ts. Takes mine address as param. If wallet connected: fetches staked balance via Staking.getStakeBalance(mine, user), pending rewards via Staking.getRewards(mine, user), user's MINER token balance (for staking). Returns: stakedBalance, pendingRewards, minerBalance, loading, error, refetch.",
      "priority": 21,
      "passes": true,
      "acceptance": "Hook fetches staking data for a specific mine and user.",
      "model": "sonnet"
    },
    {
      "id": "S74",
      "title": "Create StakingPanel component",
      "description": "Create src/components/StakingPanel.tsx. Props: mineAddress. Three sections: (1) STAKE tab \u2014 TokenInput for MINER amount, stake button. Flow: increaseAllowance on MINER for Staking contract \u2192 simulate stake(mine, amount) \u2192 sendTransaction. (2) UNSTAKE tab \u2014 TokenInput for staked amount (max = stakedBalance), unstake button. (3) CLAIM section \u2014 shows pending rewards amount, claim button. Flow: simulate claim(mine) \u2192 sendTransaction. Tab switching between stake/unstake. Display current staked balance and pending rewards.",
      "priority": 21,
      "passes": true,
      "acceptance": "Panel handles stake/unstake/claim flows. Tab switching works. All transactions use null signers.",
      "model": "sonnet"
    },
    {
      "id": "S75",
      "title": "Create StakingPage",
      "description": "Create src/pages/StakingPage.tsx. Mine selector dropdown (from useMines). When mine selected: show StakingPanel for that mine. Also show mine stats (ratio, fees) as context. If no wallet connected, show 'Connect wallet to stake' message.",
      "priority": 21,
      "passes": true,
      "acceptance": "Page allows selecting a mine and interacting with staking. Wallet required message shown when disconnected.",
      "model": "sonnet"
    },
    {
      "id": "S76",
      "title": "Add loading skeletons and spinners",
      "description": "Add loading states to all pages: HomePage shows skeleton cards (animated pulse bg-gray-800 rectangles). MineDetailPage shows skeleton stat cards. WrapPage/UnwrapPage show skeleton input areas. StakingPage shows skeleton panel. Add a global Spinner component (animated border-t-2 rounded-full).",
      "priority": 22,
      "passes": true,
      "acceptance": "All pages show appropriate loading states instead of blank screens.",
      "model": "sonnet"
    },
    {
      "id": "S77",
      "title": "Add toast notifications",
      "description": "Create a simple toast notification system. Create src/components/Toast.tsx and a useToast hook (or context). Toast types: success (green), error (red), info (blue). Auto-dismiss after 5 seconds. Position: bottom-right. Add toasts to all transaction flows: 'Transaction submitted', 'Transaction confirmed', 'Transaction failed: {error}'. Add toast for wallet connect/disconnect.",
      "priority": 22,
      "passes": true,
      "acceptance": "Toasts appear for all transaction outcomes and wallet events. Auto-dismiss works.",
      "model": "sonnet"
    },
    {
      "id": "S78",
      "title": "Add error handling for contract reverts",
      "description": "In all transaction flows (wrap, unwrap, stake, unstake, claim), catch errors from simulation and sendTransaction. Parse revert messages from error objects. Display human-readable error in toast: 'zero amount', 'not owner', 'no fee', 'no rewards', 'insufficient balance'. Handle 'Insufficient UTXOs' error with helpful message about wallet balance.",
      "priority": 22,
      "passes": true,
      "acceptance": "Contract revert messages displayed clearly to user. Common errors have helpful messages.",
      "model": "sonnet"
    },
    {
      "id": "S79",
      "title": "Mobile responsive adjustments",
      "description": "Review all pages on mobile viewport (375px width). Ensure: Navbar collapses to hamburger menu. Mine grid is single column. Input fields are full width. Buttons are full width on mobile. Stat cards stack vertically. Modal/popup fits screen. Text doesn't overflow. Touch targets are at least 44px.",
      "priority": 23,
      "passes": true,
      "acceptance": "All pages usable on 375px width viewport. No horizontal scroll. Touch-friendly.",
      "model": "sonnet"
    },
    {
      "id": "S80",
      "title": "Build frontend for production",
      "description": "Run npm run build in frontend directory. Fix any TypeScript errors. Verify dist/ output exists. Test with npm run preview \u2014 all pages load, routing works, dark theme renders. Verify base: './' in vite.config.ts for IPFS compatibility.",
      "priority": 23,
      "passes": true,
      "acceptance": "npm run build succeeds. dist/ contains index.html and assets. Preview serves correctly.",
      "model": "sonnet"
    },
    {
      "id": "S81",
      "title": "End-to-end test: mint and wrap flow",
      "description": "Using the deployed frontend and testnet contracts: (1) Connect OPWallet, (2) Navigate to WrapPage, (3) Call MinerToken.mine() to get test MINER tokens (add a 'Faucet' button or use the mine selector), (4) Enter amount and wrap MINER \u2192 xMINER, (5) Verify xToken balance appears. Document any issues in progress.txt.",
      "priority": 24,
      "passes": true,
      "acceptance": "Full mint\u2192wrap flow works in browser. xToken balance visible after wrap.",
      "model": "sonnet"
    },
    {
      "id": "S82",
      "title": "End-to-end test: unwrap flow",
      "description": "Continuing from S81: (1) Navigate to UnwrapPage, (2) Enter xToken amount, (3) Unwrap xMINER \u2192 MINER, (4) Verify MINER balance increased (minus fees), (5) Verify ratio changed. Document results.",
      "priority": 24,
      "passes": true,
      "acceptance": "Full unwrap flow works. Balances update correctly. Fees deducted properly.",
      "model": "sonnet"
    },
    {
      "id": "S83",
      "title": "End-to-end test: staking flow",
      "description": "Continuing: (1) Navigate to StakingPage, (2) Select the Mine, (3) Stake MINER tokens, (4) Verify staked balance shows, (5) Have another wallet wrap to generate fees (or skip if single-user test), (6) Check pending rewards, (7) Claim rewards. Document results.",
      "priority": 24,
      "passes": true,
      "acceptance": "Stake/claim flow works end-to-end. Staked balance and rewards display correctly.",
      "model": "sonnet"
    },
    {
      "id": "S84",
      "title": "Add MinerToken faucet button to frontend",
      "description": "Add a 'Get Test MINER' or 'Faucet' button to the HomePage or WrapPage. When clicked: calls MinerToken.mine() to mint 1000 MINER to the connected wallet. Shows success toast with minted amount. This makes testing easy for judges.",
      "priority": 25,
      "passes": true,
      "acceptance": "Faucet button visible, mints MINER tokens on click, shows confirmation.",
      "model": "sonnet"
    },
    {
      "id": "S85",
      "title": "Deploy frontend to IPFS or Vercel",
      "description": "Build frontend (npm run build). Deploy dist/ to IPFS (e.g., via Pinata, web3.storage) or Vercel. Get public URL. Test all pages work from deployed URL. Verify wallet connection works from deployed domain.",
      "priority": 25,
      "passes": true,
      "acceptance": "Frontend accessible at public URL. All features work including wallet connection.",
      "model": "sonnet"
    },
    {
      "id": "S86",
      "title": "Create submission materials",
      "description": "Take screenshots of: (1) HomePage with mine list, (2) WrapPage with amount entered, (3) MineDetailPage with stats, (4) StakingPage. Write 2-3 sentence app description. Ensure GitHub repo is public with README. Prepare tweet text with #opnetvibecode @opnetbtc.",
      "priority": 25,
      "passes": true,
      "acceptance": "Screenshots saved. Description written. GitHub repo ready. Tweet drafted.",
      "model": "sonnet"
    },
    {
      "id": "S87",
      "title": "Create deploy-mine-moto.ts script",
      "description": "Create mines-opnet/contracts/scripts/deploy-mine-moto.ts. Reuses existing Mine.wasm. UNDERLYING_ADDRESS = process.env.MOTO_UNDERLYING. MINE_NAME = 'xMoto', MINE_SYMBOL = 'xMOTO'. Same fees as other mines (WRAP_FEE=50n, UNWRAP_FEE=50n, CONTROLLER_FEE=100n, PROTOCOL_FEE=100n). Add MOTO_UNDERLYING=0xfd4473840751d58d9f8b73bdd57d6c5260453d5518bd7cd02d0a4cf3df9bf4dd to .env.",
      "priority": 26,
      "passes": true,
      "acceptance": "Script created. MOTO_UNDERLYING set in .env.",
      "model": "sonnet"
    },
    {
      "id": "S88",
      "title": "Create register-mine-moto.ts script",
      "description": "Create mines-opnet/contracts/scripts/register-mine-moto.ts. Calls Factory.registerMine(MOTO_UNDERLYING, MOTO_MINE_ADDRESS). Uses FACTORY_ADDRESS, MOTO_UNDERLYING, MOTO_MINE_ADDRESS from .env.",
      "priority": 26,
      "passes": true,
      "acceptance": "Script created. Reads MOTO_MINE_ADDRESS from .env.",
      "model": "sonnet"
    },
    {
      "id": "S89",
      "title": "Deploy xMoto Mine to testnet",
      "description": "Run: cd mines-opnet/contracts && npx ts-node scripts/deploy-mine-moto.ts. Capture contractAddress (bech32) and contractPubKey (0x hex). Add to .env: MOTO_MINE_ADDRESS=<pubkey> and MOTO_MINE_BECH32=<bech32>.",
      "priority": 26,
      "passes": true,
      "acceptance": "Deploy tx confirmed. MOTO_MINE_ADDRESS and MOTO_MINE_BECH32 added to .env. Address: opt1sqzet9gfcfyvh50qvlw5d2p0cdzndg9spa5zgr238",
      "model": "sonnet"
    },
    {
      "id": "S90",
      "title": "Register xMoto Mine with Factory",
      "description": "Run: npx ts-node scripts/register-mine-moto.ts. Confirm register tx broadcasts. Update mines-opnet/frontend/src/config.ts: add motoTokenPubkey, xMotoMine (MOTO_MINE_BECH32), xMotoMinePubkey (MOTO_MINE_ADDRESS).",
      "priority": 26,
      "passes": true,
      "acceptance": "registerMine tx confirmed (2268a7e9...). config.ts updated with xMoto addresses.",
      "model": "sonnet"
    },
    {
      "id": "S91",
      "title": "Add Wrap button to MineCard",
      "description": "Modify mines-opnet/frontend/src/components/MineCard.tsx. Add a 'Wrap' button at the bottom of the card that links to /wrap/:address. Use e.stopPropagation() so click does not bubble to the outer Link. Rename label 'Underlying Balance' to 'Total Wrapped'.",
      "priority": 26,
      "passes": true,
      "acceptance": "MineCard shows 'Total Wrapped' label and a 'Wrap' button linking to /wrap/:address.",
      "model": "sonnet"
    },
    {
      "id": "S92",
      "title": "Verify xMoto card on HomePage",
      "description": "Load frontend. Confirm xMoto card appears in the mines grid (fetched via useMines from Factory). Card shows name 'xMoto', symbol 'xMOTO', Total Wrapped balance (0 initially), and a working Wrap button that navigates to /wrap/<xMoto-mine-address> with Moto as the underlying token input.",
      "priority": 26,
      "passes": true,
      "acceptance": "xMoto card visible. Wrap button navigates correctly. WrapPage loads Moto as underlying.",
      "model": "sonnet"
    },
    {
      "id": "S93",
      "title": "Remove controller/protocol fee storage from Mine.ts",
      "description": "Edit contracts/src/mine/Mine.ts. (1) Delete the 4 storage pointer declarations: _controllerFee, _protocolFee, _controllerFeeAccrued, _protocolFeeAccrued. (2) Rewrite the WrapResult class to hold exactly 2 fields: xAmount: u256 and feeAmount: u256 \u2014 remove stakersFee, controllerFeeAmount, protocolFeeAmount fields and update the constructor accordingly. (3) Rewrite _underlyingBalance() to simply return this.lu(this.fieldKeySimple(this._underlyingHeld)) \u2014 remove all subtraction of accrued fees. Do NOT compile yet \u2014 further edits follow in S94\u2013S101.",
      "priority": 27,
      "passes": true,
      "acceptance": "WrapResult class has exactly 2 fields (xAmount, feeAmount). _underlyingBalance() has no references to _controllerFeeAccrued or _protocolFeeAccrued. No references to the 4 removed storage pointer names anywhere in Mine.ts.",
      "model": "sonnet"
    },
    {
      "id": "S94",
      "title": "Remove controller/protocol fee methods from Mine.ts",
      "description": "Edit contracts/src/mine/Mine.ts. Delete the following 9 methods entirely (including their @method() and @returns() decorators): claimControllerFee, claimProtocolFee, disburseProtocolFee, setControllerFee, setProtocolFee, getControllerFee, getProtocolFee, getControllerFeeAccrued, getProtocolFeeAccrued. Also delete the constant MAX_CONTROLLER_PROTOCOL_FEE at the top of the file. Do NOT compile yet.",
      "priority": 27,
      "passes": true,
      "acceptance": "None of the 9 method names appear in Mine.ts. MAX_CONTROLLER_PROTOCOL_FEE constant is gone. All other methods remain intact: wrap, unwrap, getWrappedAmount, getUnwrappedAmount, underlyingBalance, getUnderlyingAmount, getWrapFee, getUnwrapFee, setWrapFee, setUnwrapFee, getOwner, getUnderlying.",
      "model": "sonnet"
    },
    {
      "id": "S95",
      "title": "Simplify _calcWrap() and wrap() in Mine.ts \u2014 fees stay in pool",
      "description": "Edit contracts/src/mine/Mine.ts. Rewrite _calcWrap() as follows: if supply==ZERO return new WrapResult(amount, ZERO) (first wrap 1:1 no fee). Otherwise: feeAmount = SafeMath.div(SafeMath.mul(amount, wrapFee), FEE_DENOMINATOR); netAmount = SafeMath.sub(amount, feeAmount); xAmount = SafeMath.div(SafeMath.mul(supply, netAmount), this._underlyingBalance()); return new WrapResult(xAmount, feeAmount). The feeAmount stays in the pool (not split out). Simplify wrap(): remove all ctrlKey/protoKey accumulator updates. Keep: TransferHelper.transferFrom to pull underlying in, update _underlyingHeld += amount (full amount including fee), mint xTokens. Do NOT compile yet.",
      "priority": 27,
      "passes": true,
      "acceptance": "_calcWrap() has no references to controllerFee, protocolFee, stakersFee, ctrlKey, protoKey, or any accumulator. wrap() has no accumulator update lines. _underlyingHeld is incremented by the full amount (fee included). First wrap is 1:1; subsequent wraps yield fewer xTokens relative to amount deposited.",
      "model": "sonnet"
    },
    {
      "id": "S96",
      "title": "Simplify unwrap() in Mine.ts \u2014 fee stays in pool",
      "description": "Edit contracts/src/mine/Mine.ts. Rewrite unwrap() as follows: (1) keep: underlyingAmount = _getUnderlyingAmount(xAmount). (2) keep: feeAmount = SafeMath.div(SafeMath.mul(underlyingAmount, unwrapFeeRate), FEE_DENOMINATOR). (3) keep: netUnderlying = SafeMath.sub(underlyingAmount, feeAmount). (4) keep: _burn sender xAmount. (5) REMOVE: all controllerFeeAmount, protocolFeeAmount calculations and ctrlKey/protoKey accumulator updates. (6) keep: _underlyingHeld decremented by netUnderlying ONLY (feeAmount remains in pool permanently). (7) keep: TransferHelper.transfer netUnderlying to sender. Do NOT compile yet.",
      "priority": 27,
      "passes": true,
      "acceptance": "unwrap() has no references to controllerFee, protocolFee, ctrlKey, protoKey, controllerFeeAmount, or protocolFeeAmount. _underlyingHeld is decremented only by netUnderlying. Sender receives underlyingAmount*(1 - unwrapFee/1000). feeAmount stays locked in contract forever, growing the pool ratio.",
      "model": "sonnet"
    },
    {
      "id": "S97",
      "title": "Update Mine.ts onDeployment() \u2014 remove old fee params",
      "description": "Edit contracts/src/mine/Mine.ts. In onDeployment(): remove the two _calldata.readU256() lines for controllerFee and protocolFee. Remove the two this.su() calls that stored them. After this change onDeployment() reads exactly 6 calldata values in this order: underlying (readAddress), decimals (readU8), name (readStringWithLength), symbol (readStringWithLength), wrapFee (readU256), unwrapFee (readU256). Do NOT compile yet.",
      "priority": 27,
      "passes": true,
      "acceptance": "onDeployment() contains exactly 6 calldata reads (1 Address, 1 u8, 2 strings, 2 u256). No controllerFee or protocolFee variable or su() call present. Calldata order is: underlying \u2192 decimals \u2192 name \u2192 symbol \u2192 wrapFee \u2192 unwrapFee.",
      "model": "sonnet"
    },
    {
      "id": "S98",
      "title": "Add AMM pool whitelist storage and setter/getter to Mine.ts",
      "description": "Edit contracts/src/mine/Mine.ts. (1) Add storage pointer: private readonly _ammPool: u16 = Blockchain.nextPointer; (after the existing pointer declarations). (2) Add private requireAmmPool(): void \u2014 reads _ammPool via la(); if Blockchain.tx.sender != storedPool throw new Revert('not amm pool'). (3) Add @method() setAmmPool(_calldata): BytesWriter \u2014 requireOwner(); reads Address from calldata via readAddress(); stores via sa(fieldKeySimple(_ammPool), addr); returns BytesWriter with writeBoolean(true). @returns({ name: 'success', type: ABIDataTypes.BOOL }). (4) Add @method() getAmmPool(_calldata): BytesWriter \u2014 reads and returns Address. @returns({ name: 'pool', type: ABIDataTypes.ADDRESS }). Do NOT compile yet.",
      "priority": 27,
      "passes": true,
      "acceptance": "_ammPool storage pointer declared. requireAmmPool() helper exists and reverts 'not amm pool' for non-pool callers. setAmmPool() has requireOwner() guard. getAmmPool() returns stored address. Both new methods have @method() decorator.",
      "model": "sonnet"
    },
    {
      "id": "S99",
      "title": "Add AMM buy/sell fee storage, setters, and getters to Mine.ts",
      "description": "Edit contracts/src/mine/Mine.ts. (1) Add 2 storage pointers: private readonly _ammBuyFee: u16 = Blockchain.nextPointer; and private readonly _ammSellFee: u16 = Blockchain.nextPointer;. (2) Add setAmmBuyFee(_calldata): requireOwner(); read u256; if gt(fee, MAX_DEPOSIT_WITHDRAW_FEE) throw Revert('fee too high'); store; return bool true. (3) Add setAmmSellFee(_calldata): same pattern. (4) Add getAmmBuyFee(_calldata): return lu(_ammBuyFee). (5) Add getAmmSellFee(_calldata): return lu(_ammSellFee). All 4 decorated with @method() and @returns(). setters return BOOL, getters return UINT256. Do NOT compile yet.",
      "priority": 27,
      "passes": true,
      "acceptance": "_ammBuyFee and _ammSellFee storage pointers declared. Both setters have requireOwner() and validate against MAX_DEPOSIT_WITHDRAW_FEE. Both getters return stored u256 (0 if unset). All 4 methods have @method() decorator.",
      "model": "sonnet"
    },
    {
      "id": "S100",
      "title": "Add notifyAmmFee() method to Mine.ts",
      "description": "Edit contracts/src/mine/Mine.ts. Add notifyAmmFee(_calldata: Calldata): BytesWriter with @method() @returns({ name: 'amount', type: ABIDataTypes.UINT256 }). Implementation (CEI order): CHECKS \u2014 call requireAmmPool(); read amount: u256 = _calldata.readU256(); if amount==ZERO throw Revert('zero amount'). EFFECTS \u2014 read heldKey; su(heldKey, SafeMath.add(lu(heldKey), amount)). INTERACTIONS \u2014 read underlying via la(_underlying); TransferHelper.transferFrom(underlying, Blockchain.tx.sender, Blockchain.contractAddress, amount). Return BytesWriter(32) with writeU256(amount). This function is called by the whitelisted AMM pool AFTER it has approved the Mine contract to spend the fee tokens. Do NOT compile yet.",
      "priority": 27,
      "passes": true,
      "acceptance": "notifyAmmFee() exists with @method() decorator. Calling from non-pool address reverts 'not amm pool'. Calling with 0 reverts 'zero amount'. On success: _underlyingHeld increases by amount; underlying tokens are pulled from caller via transferFrom. CEI order respected: held update happens before transferFrom call.",
      "model": "sonnet"
    },
    {
      "id": "S101",
      "title": "Update Mine onDeployment() for AMM params, compile Mine, update ABIs",
      "description": "Edit contracts/src/mine/Mine.ts onDeployment(): after reading unwrapFee add: const ammBuyFee: u256 = _calldata.readU256(); const ammSellFee: u256 = _calldata.readU256(); then store both: this.su(this.fieldKeySimple(this._ammBuyFee), ammBuyFee); this.su(this.fieldKeySimple(this._ammSellFee), ammSellFee);. Final calldata order: underlying, decimals, name, symbol, wrapFee, unwrapFee, ammBuyFee, ammSellFee (8 values). Then compile the Mine target: cd contracts && npx asc src/mine/index.ts --config asconfig.json --target mine (check asconfig.json for exact target name). Then manually update contracts/abis/Mine.abi.json, Mine.abi.ts, Mine.d.ts to reflect the new method set (add AMM methods, remove old fee-split methods).",
      "priority": 27,
      "passes": true,
      "acceptance": "onDeployment() reads 8 calldata values. npx asc exits 0. Mine.abi.json contains: wrap, unwrap, getWrappedAmount, getUnwrappedAmount, underlyingBalance, getUnderlyingAmount, getWrapFee, getUnwrapFee, setWrapFee, setUnwrapFee, getOwner, getUnderlying, setAmmPool, getAmmPool, setAmmBuyFee, setAmmSellFee, getAmmBuyFee, getAmmSellFee, notifyAmmFee. Mine.abi.json does NOT contain: claimControllerFee, claimProtocolFee, disburseProtocolFee, setControllerFee, setProtocolFee, getControllerFee, getProtocolFee. Mine.d.ts interface updated to match.",
      "model": "sonnet"
    },
    {
      "id": "S102",
      "title": "Delete Staking contract files and ABIs",
      "description": "Delete the following files/directories: contracts/src/staking/Staking.ts, contracts/src/staking/index.ts, contracts/src/staking/ (directory), contracts/abis/Staking.abi.json, contracts/abis/Staking.abi.ts, contracts/abis/Staking.d.ts, contracts/scripts/deploy-staking.ts. After deletion, grep contracts/ for any remaining imports of staking and remove them.",
      "priority": 27,
      "passes": true,
      "acceptance": "None of the listed paths exist on disk. contracts/src/ has no staking/ subdirectory. No file in contracts/ imports from ./staking or ../staking. contracts/abis/ has no Staking.* files.",
      "model": "sonnet"
    },
    {
      "id": "S103",
      "title": "Remove staking from contracts build config",
      "description": "Edit contracts/asconfig.json: remove the 'staking' entry from the targets object. All other targets (miner, mine, factory) must remain unchanged. Edit contracts/package.json: remove any npm script that references 'staking' (e.g. build:staking, deploy:staking). Do not remove other scripts.",
      "priority": 27,
      "passes": true,
      "acceptance": "contracts/asconfig.json has no 'staking' key under targets. contracts/package.json has no script name or command referencing 'staking'. All non-staking targets and scripts are unchanged.",
      "model": "sonnet"
    },
    {
      "id": "S104",
      "title": "Remove staking from frontend and verify build",
      "description": "Delete files: frontend/src/pages/StakingPage.tsx, frontend/src/components/StakingPanel.tsx, frontend/src/hooks/useStaking.ts. Edit frontend/src/App.tsx: remove the StakingPage import line and the /staking Route entry. Edit frontend/src/components/Navbar.tsx: remove the staking nav link (the <Link to='/staking'> element or equivalent). Then run: cd frontend && npm run build.",
      "priority": 27,
      "passes": true,
      "acceptance": "The 3 deleted files do not exist. App.tsx has no StakingPage import or /staking Route. Navbar.tsx has no /staking link. npm run build exits 0 with no TypeScript errors. grep -r 'useStaking\\|StakingPanel\\|StakingPage' frontend/src returns no results.",
      "model": "sonnet"
    },
    {
      "id": "S105",
      "title": "Remove mine() faucet from MinerToken.ts, rebuild, update ABIs",
      "description": "Edit contracts/src/miner-token/MinerToken.ts: delete the entire mine(_calldata: Calldata): BytesWriter method including its @method() and @returns() decorators. Compile the miner target: cd contracts && npx asc src/miner-token/index.ts --config asconfig.json --target miner (check asconfig.json for exact target name). Update contracts/abis/MinerToken.abi.json to remove the 'mine' selector entry. Update MinerToken.abi.ts and MinerToken.d.ts accordingly.",
      "priority": 27,
      "passes": true,
      "acceptance": "MinerToken.ts has no mine() method. npx asc exits 0. MinerToken.abi.json does not contain a 'mine' selector. MinerToken.d.ts has no mine method signature. Standard OP20 methods remain in ABI.",
      "model": "sonnet"
    },
    {
      "id": "S106",
      "title": "Remove faucet from frontend and verify build",
      "description": "Delete file: frontend/src/pages/FaucetPage.tsx. Edit frontend/src/App.tsx: remove the FaucetPage import and the /faucet Route entry. Edit frontend/src/pages/HomePage.tsx: remove the faucet button and any UI related to MinerToken minting / 'Get MINER' button that was added in S84. Then run: cd frontend && npm run build.",
      "priority": 27,
      "passes": true,
      "acceptance": "FaucetPage.tsx does not exist. App.tsx has no FaucetPage import or /faucet Route. HomePage.tsx has no faucet button or reference to the mine() method. npm run build exits 0 with no TypeScript errors. grep -r 'FaucetPage\\|faucet' frontend/src returns no results.",
      "model": "sonnet"
    },
    {
      "id": "S107",
      "title": "Update all Mine deploy scripts for new constructor signature",
      "description": "Edit the following 5 scripts: contracts/scripts/deploy-mine.ts, deploy-mine-crystal.ts, deploy-mine-lava.ts, deploy-mine-moto.ts, deploy-mine-storm.ts. Per script: (1) Remove CONTROLLER_FEE constant and its calldata encoding. (2) Remove PROTOCOL_FEE constant and its calldata encoding. (3) Add const AMM_BUY_FEE = 50n; and const AMM_SELL_FEE = 50n; (4) Update calldata encoding to match new onDeployment() order: [underlying (Address bytes), decimals (u8), name (string with length prefix), symbol (string with length prefix), wrapFee (u256 BE), unwrapFee (u256 BE), ammBuyFee (u256 BE), ammSellFee (u256 BE)]. Keep existing wrapFee=25n and unwrapFee=50n defaults. Verify with: cd contracts && npx tsc --noEmit.",
      "priority": 27,
      "passes": true,
      "acceptance": "All 5 scripts have no CONTROLLER_FEE or PROTOCOL_FEE references. All 5 have AMM_BUY_FEE=50n and AMM_SELL_FEE=50n. Calldata encodes exactly 8 args in the specified order. npx tsc --noEmit exits 0 with no errors.",
      "model": "sonnet"
    },
    {
      "id": "S108",
      "title": "Foundation: Fonts, CSS variables, global reset",
      "description": "Apply the HELIX design foundation across 3 files. (1) frontend/index.html: add Google Fonts preconnect + stylesheet link for Sometype Mono (wght 400;500;700) and Mulish (wght 400;700;800). Change <title> to 'HELIX'. (2) frontend/src/index.css: replace entire file with: :root { --color-bg:#fff; --color-fg:#000; --color-border:#000; --color-muted:#888; font-family:'Sometype Mono',monospace; background:#fff; color:#000; line-height:1.5; } * { box-sizing:border-box; margin:0; padding:0; border-radius:0!important; } a { color:#000; text-decoration:none; } a:hover { text-decoration:underline; } button { border-radius:0; border:1px solid #000; background:transparent; color:#000; cursor:pointer; font-family:'Sometype Mono',monospace; } body { min-width:320px; min-height:100vh; } (3) frontend/src/styles/globals.css: keep @import 'tailwindcss' line and walletconnect fix CSS unchanged. In the @layer base block replace 'bg-gray-950 text-gray-100 font-sans' with 'bg-white text-black'. Append at end of file: @layer base { * { border-radius: 0 !important; } h1,h2,h3,h4,h5,h6 { font-family: 'Mulish', sans-serif; } }. Run: cd frontend && npm run build.",
      "priority": 28,
      "passes": true,
      "acceptance": "npm run build exits 0. index.html has Google Fonts links for Sometype Mono and Mulish. Title is HELIX. index.css has CSS vars --color-bg --color-fg --color-border --color-muted. globals.css @layer base uses bg-white text-black. Body background is white, text is black. No border-radius on any element.",
      "model": "sonnet"
    },
    {
      "id": "S109",
      "title": "Navbar redesign \u2014 industrial HELIX brand, slash-prefixed links",
      "description": "Rewrite frontend/src/components/Navbar.tsx. Change nav link labels to '/Wrap' and '/Unwrap' (slash is part of label). Remove 'Home' link. Nav element style: borderBottom:'1px solid #000', background:'#fff', position:'sticky', top:0, zIndex:100. Inner container div style: maxWidth:'900px', margin:'0 auto', height:'48px', display:'flex', alignItems:'center', justifyContent:'space-between', padding:'0 2rem'. Brand Link to '/' style: fontFamily:'Mulish', fontWeight:800, fontSize:'1.1rem', textTransform:'uppercase', letterSpacing:'0.05em', color:'#000', textDecoration:'none', text 'HELIX'. Desktop links: display flex gap:24px. Each Link style: fontFamily:'Sometype Mono', fontSize:'0.8rem', color:'#000', textDecoration:'none'. On hover: textDecoration:'underline' only, no background change. Mobile menu div style: borderTop:'1px solid #000', padding:'12px 2rem', background:'#fff', display:'flex', flexDirection:'column', gap:'12px'. Remove all Tailwind classes with bg-gray-900, border-gray-800, text-gray-400, text-white, hover:text-white, rounded. Run: cd frontend && npm run build.",
      "priority": 28,
      "passes": true,
      "acceptance": "npm run build exits 0. Navbar: white bg, 1px black bottom border, 48px height. Brand 'HELIX' in Mulish 800 uppercase. Nav links '/Wrap' and '/Unwrap' in Sometype Mono. No gray or purple colors. Mobile hamburger collapses links correctly.",
      "model": "sonnet"
    },
    {
      "id": "S110",
      "title": "Layout redesign \u2014 white bg, 900px container, left rail",
      "description": "Rewrite frontend/src/components/Layout.tsx. Outer wrapper: style={{ position:'relative', minHeight:'100vh', background:'#fff', color:'#000', display:'flex', flexDirection:'column' }}. Remove className 'flex flex-col min-h-screen bg-gray-950 text-gray-100'. Add left rail div as first child inside outer wrapper (before Navbar): <div aria-hidden='true' style={{ position:'absolute', left:'calc((100vw - 900px) / 2 - 28px)', top:'80px', width:'1px', background:'#000', height:'60vh', pointerEvents:'none' }} />. Main element: style={{ maxWidth:'900px', margin:'0 auto', padding:'0 2rem', flex:1, width:'100%' }}. Remove className 'flex-1' from main. Keep Navbar at top inside outer wrapper. Run: cd frontend && npm run build.",
      "priority": 28,
      "passes": true,
      "acceptance": "npm run build exits 0. Pages show white background, black text. Content centered at max 900px with 2rem horizontal padding. A 1px black vertical line appears left of the content area (starts 80px from top, 60vh tall). No gray backgrounds in Layout.",
      "model": "sonnet"
    },
    {
      "id": "S111",
      "title": "MineCard redesign \u2014 bordered card with corner bracket motifs",
      "description": "Rewrite frontend/src/components/MineCard.tsx styles. Outer div: style={{ position:'relative', border:'1px solid #000', background:'#fff', padding:'20px', cursor:'pointer' }}. Remove rounded-xl, bg-gray-900, hover:bg-gray-800, border-gray-800, transition-colors. Add top-right bracket as first child: <div aria-hidden='true' style={{ position:'absolute', top:'-1px', right:'-1px', width:'16px', height:'16px', borderTop:'1px solid #000', borderRight:'1px solid #000' }} />. Add bottom-left bracket as last child before closing outer div: <div aria-hidden='true' style={{ position:'absolute', bottom:'-1px', left:'-1px', width:'16px', height:'16px', borderBottom:'1px solid #000', borderLeft:'1px solid #000' }} />. Mine name h3: style={{ fontFamily:'Mulish', fontWeight:700, fontSize:'1rem', color:'#000' }}. Remove text-lg, font-semibold, text-white. Symbol span: style={{ fontSize:'0.8rem', color:'#888' }}. Address span: style={{ fontSize:'0.7rem', color:'#888', fontFamily:'Sometype Mono' }}. Stat labels span: color:'#888', fontSize:'0.8rem'. Stat values span: color:'#000', fontSize:'0.8rem'. Remove text-gray-400, text-white, text-purple-400, text-blue-400. Stats container: display:'flex', flexDirection:'column', gap:'6px', margin:'12px 0'. Each stat row: display:'flex', justifyContent:'space-between'. Wrap Link: add hovered state via useState, style={{ display:'block', width:'100%', textAlign:'center', border:'1px solid #000', background: hovered ? '#000' : '#fff', color: hovered ? '#fff' : '#000', padding:'8px', fontFamily:'Sometype Mono', fontSize:'0.8rem', marginTop:'16px', textDecoration:'none' }}, onMouseEnter/Leave toggles hovered. Remove bg-purple-600, rounded-lg from Link. Run: cd frontend && npm run build.",
      "priority": 29,
      "passes": true,
      "acceptance": "npm run build exits 0. MineCard: white bg, 1px black border, zero radius. Corner brackets at top-right and bottom-left. All text #000 or #888. No purple, blue, or dark backgrounds. Wrap button outlined, instant hover invert. Mine name in Mulish 700.",
      "model": "sonnet"
    },
    {
      "id": "S112",
      "title": "TokenInput + TransactionButton redesign \u2014 industrial form controls",
      "description": "Rewrite styles in 2 files. (1) frontend/src/components/TokenInput.tsx: Label style: fontSize:'0.75rem', color:'#888', fontFamily:'Sometype Mono', display:'block', marginBottom:'4px'. Max span style: fontSize:'0.7rem', color:'#888'. Input container div style: display:'flex', alignItems:'stretch', border:'1px solid #000', background:'#fff'. Remove bg-gray-800, border-gray-700, rounded-lg, focus-within classes, overflow-hidden. Input element style: flex:1, background:'transparent', border:'none', outline:'none', padding:'10px 12px', color:'#000', fontFamily:'Sometype Mono', fontSize:'0.9rem'. Remove text-white, placeholder-gray-600, appearance classes. MAX button style: borderLeft:'1px solid #000', background:'transparent', color:'#000', padding:'4px 10px', fontFamily:'Sometype Mono', fontSize:'0.7rem', cursor:'pointer', border:'none' then override borderLeft:'1px solid #000' (use a style object with only borderLeft). Remove text-purple-400, hover:bg-gray-700, rounded, mr-1. Symbol badge span style: borderLeft:'1px solid #000', padding:'10px 12px', fontSize:'0.8rem', color:'#000', background:'#fff', whiteSpace:'nowrap', display:'flex', alignItems:'center'. Remove bg-gray-750, text-gray-300. (2) frontend/src/components/TransactionButton.tsx: Add const [hovered, setHovered] = useState(false). Button style: border:'1px solid #000', background: hovered && !disabled ? '#000' : '#fff', color: hovered && !disabled ? '#fff' : '#000', width:'100%', padding:'10px 16px', fontFamily:'Sometype Mono', fontSize:'0.8rem', cursor: disabled ? 'not-allowed' : 'pointer', opacity: disabled ? 0.4 : 1. Add onMouseEnter/Leave for hovered state. Remove all purple gradient, rounded-lg, transition classes. Run: cd frontend && npm run build.",
      "priority": 29,
      "passes": true,
      "acceptance": "npm run build exits 0. TokenInput: 1px black border container, white bg, black text, MAX button with left-border separator, symbol badge with left-border separator, no radius, no purple. TransactionButton: outlined black border white bg, instant hover invert, 40% opacity when disabled.",
      "model": "sonnet"
    },
    {
      "id": "S113",
      "title": "HomePage redesign \u2014 section heading with glyph, tick divider, monochrome grid",
      "description": "Rewrite frontend/src/pages/HomePage.tsx visual layer. (1) Page container: style={{ padding:'48px 0' }}. Remove className 'max-w-6xl mx-auto p-4 md:p-6'. (2) Replace entire heading block with: <div style={{ marginBottom:'16px' }}><h1 style={{ display:'flex', alignItems:'center', gap:'12px', fontFamily:'Mulish', fontWeight:700, fontSize:'1.5rem', color:'#000', lineHeight:1.1 }}><span style={{ fontFamily:'Sometype Mono', fontSize:'1rem', fontWeight:400 }}>&#8801;</span>Helix <strong>Genomes</strong></h1><p style={{ fontFamily:'Sometype Mono', fontSize:'0.8rem', color:'#888', marginTop:'8px' }}>Wrap OP-20 tokens into yield-bearing xTokens on Bitcoin L1.</p></div>. (3) Add tick-mark divider before mine grid: <div style={{ backgroundImage:'repeating-linear-gradient(to right, #000 0px, #000 1px, transparent 1px, transparent 6px)', height:'16px', width:'100%', margin:'32px 0' }} />. (4) Skeleton card outer: style={{ border:'1px solid #eee', background:'#fff', padding:'20px' }}, inner skeleton blocks: style={{ background:'#eee', height:'NNpx', marginBottom:'8px', borderRadius:0 }}. Remove bg-gray-900, bg-gray-800, rounded-xl. (5) Empty state: style={{ textAlign:'center', padding:'80px 0', color:'#888', fontFamily:'Sometype Mono' }}. Remove text-gray-500. (6) Error block: style={{ border:'1px solid #000', padding:'16px', marginBottom:'24px', fontFamily:'Sometype Mono', fontSize:'0.8rem', color:'#000' }}, prepend '[!] ' to message. Remove bg-red-900/30, border-red-700, text-red-300, rounded-lg. Run: cd frontend && npm run build.",
      "priority": 29,
      "passes": true,
      "acceptance": "npm run build exits 0. HomePage: heading shows glyph prefix + 'Helix Genomes' in Mulish 700. Tick-mark divider appears between heading and grid. Skeleton uses white/#eee. Error is bordered monochrome box with [!] prefix. No dark backgrounds or purple colors.",
      "model": "sonnet"
    },
    {
      "id": "S114",
      "title": "WrapPage + UnwrapPage redesign \u2014 industrial form layout",
      "description": "Apply identical style changes to both frontend/src/pages/WrapPage.tsx and frontend/src/pages/UnwrapPage.tsx. (1) Page container: style={{ padding:'48px 0', maxWidth:'520px', margin:'0 auto' }}. Remove 'max-w-lg mx-auto p-4 md:p-6'. (2) h1: style={{ fontFamily:'Mulish', fontWeight:700, fontSize:'1.5rem', color:'#000', marginBottom:'32px', display:'flex', alignItems:'center', gap:'10px' }}. Prepend <span style={{ fontFamily:'Sometype Mono', fontWeight:400 }}>&#xB7;&#xB7;&#xB7;</span> before title text. (3) Mine selector label: style={{ fontSize:'0.75rem', color:'#888', fontFamily:'Sometype Mono', display:'block', marginBottom:'4px' }}. (4) Select: style={{ width:'100%', border:'1px solid #000', background:'#fff', color:'#000', padding:'10px 12px', fontFamily:'Sometype Mono', fontSize:'0.85rem', outline:'none', appearance:'none' }}. Remove bg-gray-800, border-gray-700, rounded-lg. (5) Skeleton blocks: style={{ background:'#eee', height:'NNpx', marginBottom:'8px' }}. Remove bg-gray-700, bg-gray-800. (6) Preview box: style={{ border:'1px solid #000', padding:'16px', marginTop:'8px' }}. Remove bg-gray-900, rounded-lg. Preview 'Preview' label: style={{ fontSize:'0.65rem', color:'#888', fontFamily:'Sometype Mono', textTransform:'uppercase', letterSpacing:'0.1em', marginBottom:'8px' }}. Preview row labels: color:'#888'. Preview row values: color:'#000'. Remove text-gray-400, text-purple-400, text-gray-300. (7) Connect wallet message: style={{ textAlign:'center', color:'#888', fontSize:'0.85rem', fontFamily:'Sometype Mono', padding:'16px 0' }}. (8) Balance info: style={{ fontSize:'0.75rem', textAlign:'center', color:'#888', fontFamily:'Sometype Mono' }}. Run: cd frontend && npm run build.",
      "priority": 29,
      "passes": true,
      "acceptance": "npm run build exits 0. WrapPage and UnwrapPage: white background throughout. Heading has '\u00b7\u00b7\u00b7' glyph prefix in Mulish. Select has 1px black border, no radius. Preview box has 1px black border white bg. Labels #888, values #000. No bg-gray-* or purple colors.",
      "model": "sonnet"
    },
    {
      "id": "S115",
      "title": "MineDetailPage redesign \u2014 bordered stats table, outlined action buttons",
      "description": "Rewrite frontend/src/pages/MineDetailPage.tsx styles. (1) Page container: style={{ padding:'48px 0' }}. (2) Title h1/heading: style={{ fontFamily:'Mulish', fontWeight:700, fontSize:'1.5rem', color:'#000', marginBottom:'32px', display:'flex', alignItems:'center', gap:'10px' }}. Prepend <span style={{ fontFamily:'Sometype Mono', fontWeight:400 }}>|||</span>. (3) Replace stat card divs with <table style={{ borderCollapse:'collapse', width:'100%', border:'1px solid #000', marginBottom:'32px' }}>. Each stat: <tr><td style={{ border:'1px solid #000', padding:'10px 14px', fontSize:'0.8rem', fontFamily:'Sometype Mono', color:'#888', width:'40%' }}>label</td><td style={{ border:'1px solid #000', padding:'10px 14px', fontSize:'0.8rem', fontFamily:'Sometype Mono', color:'#000' }}>value</td></tr>. Stat rows: Ratio, Total Wrapped, xToken Supply, Wrap Fee %, Unwrap Fee %, Underlying Token. (4) User position section (wallet connected): same bordered table for 'Your xToken Balance' and 'Your Underlying Value'. Add section label above: style={{ fontSize:'0.7rem', color:'#888', fontFamily:'Sometype Mono', textTransform:'uppercase', letterSpacing:'0.1em', marginBottom:'8px' }} text 'Your Position'. (5) Action buttons row: style={{ display:'flex', gap:'16px', marginTop:'32px' }}. Each Link/button: style={{ flex:1, border:'1px solid #000', background: hovered ? '#000' : '#fff', color: hovered ? '#fff' : '#000', padding:'10px', fontFamily:'Sometype Mono', fontSize:'0.8rem', textAlign:'center', textDecoration:'none', display:'block', cursor:'pointer' }}, use per-button hovered state. (6) Skeleton: border:'1px solid #eee', background:'#eee'. (7) Error: bordered mono box '[!] ' prefix. Remove all bg-gray-*, text-gray-*, text-purple-*, rounded-*. Run: cd frontend && npm run build.",
      "priority": 29,
      "passes": true,
      "acceptance": "npm run build exits 0. MineDetailPage: stats in bordered table, #888 labels #000 values in Sometype Mono. '|||' glyph heading in Mulish. Action buttons outlined, instant hover invert. No dark colors or radius.",
      "model": "sonnet"
    },
    {
      "id": "S116",
      "title": "Spinner + ToastContainer redesign \u2014 monochrome black/white only",
      "description": "Redesign 2 components. (1) frontend/src/components/Spinner.tsx: Read file. Replace colored border Tailwind classes (border-purple-*, border-blue-*, border-indigo-*) with border-black and border-t-transparent (or inline style border:'2px solid #000', borderTopColor:'transparent'). Keep animate-spin class or equivalent @keyframes. borderRadius:50% is the one allowed exception to the no-radius rule for spinners. (2) frontend/src/components/ToastContainer.tsx and any Toast subcomponent inside it: Replace bg-green-*, bg-red-*, bg-blue-*, bg-emerald-* with background:'#fff'. Replace border-green-*, border-red-*, border-blue-* with border:'1px solid #000'. Replace text-green-*, text-red-*, text-blue-*, text-emerald-* with color:'#000'. Add fontFamily:'Sometype Mono', fontSize:'0.75rem' to toast item. Prepend type prefix to message: success \u2192 '[OK] ', error \u2192 '[!] ', info \u2192 '[i] '. Keep fixed positioning, z-index, and auto-dismiss timer unchanged. Run: cd frontend && npm run build.",
      "priority": 30,
      "passes": true,
      "acceptance": "npm run build exits 0. Spinner: black border with transparent top edge, monochrome. Toasts: white bg, 1px black border, black Sometype Mono text, type indicated by text prefix ([OK] / [!] / [i]). No green/red/blue/purple in either component.",
      "model": "sonnet"
    },
    {
      "id": "S117",
      "title": "WalletButton redesign \u2014 outlined monochrome, instant hover invert",
      "description": "Rewrite frontend/src/components/WalletButton.tsx. Read file first. Add: const [hovered, setHovered] = useState(false). Both connected and disconnected button style: border:'1px solid #000', background: hovered ? '#000' : '#fff', color: hovered ? '#fff' : '#000', fontFamily:'Sometype Mono', fontSize:'0.75rem', padding:'6px 16px', cursor:'pointer'. Add onMouseEnter={() => setHovered(true)} and onMouseLeave={() => setHovered(false)}. Disconnected label: 'Connect Wallet'. Connected label: truncated address (use existing truncateAddress helper or addr.slice(0,8)+'...'+addr.slice(-6)). Keep all existing onClick connect/disconnect logic unchanged. Remove className props containing bg-purple-*, bg-blue-*, bg-indigo-*, hover:bg-*, text-purple-*, text-blue-*, text-white, rounded-*, border-purple-*, border-blue-*. Do NOT add any CSS transition property. Run: cd frontend && npm run build.",
      "priority": 30,
      "passes": true,
      "acceptance": "npm run build exits 0. WalletButton: 1px black border, white bg, black text in both states. Hover instantly inverts to black bg white text (no animation delay). Sometype Mono font. No purple or blue colors. Connect/disconnect still functions.",
      "model": "sonnet"
    },
    {
      "id": "S118",
      "title": "CreateMineModal \u2014 UI shell on WrapPage",
      "description": "Add a 'Create Wrapper' button to frontend/src/pages/WrapPage.tsx (top-right area, next to the mine selector). Create frontend/src/components/CreateMineModal.tsx: a full-screen overlay modal (position:fixed, inset:0, z-index:9999, black bg at 50% opacity). Inside: a centered white box (max-width:480px, border:'1px solid #000', padding:24px, Sometype Mono font). Fields: (1) Underlying token address (text input, placeholder: 'opt1sq... token address'), (2) xToken name (text, placeholder: 'xFoo'), (3) xToken symbol (text, placeholder: 'xFOO'), (4) Wrap fee bps (number, default 50), (5) Unwrap fee bps (number, default 50). Buttons: 'Deploy Mine' (black bg, white text) and 'Cancel' (outlined). Modal opens when 'Create Wrapper' is clicked; 'Cancel' or clicking the backdrop closes it. No transaction logic yet \u2014 just UI state. Run: cd frontend && npm run build.",
      "priority": 31,
      "passes": true,
      "acceptance": "npm run build exits 0. WrapPage shows 'Create Wrapper' button. Clicking it opens modal overlay. Modal has 5 input fields and two buttons. Cancel closes modal. No transactions sent yet.",
      "model": "sonnet"
    },
    {
      "id": "S119",
      "title": "CreateMineModal \u2014 bundle Mine.wasm into frontend",
      "description": "Copy contracts/build/Mine.wasm to frontend/public/Mine.wasm. In CreateMineModal.tsx, add a loadBytecode() async function that does: const resp = await fetch('./Mine.wasm'); const buf = await resp.arrayBuffer(); return new Uint8Array(buf). Call it inside handleDeploy (not on mount). Add a console.log to confirm the WASM loaded and its byte length. Run: cd frontend && npm run build.",
      "priority": 31,
      "passes": true,
      "acceptance": "Mine.wasm is in frontend/public/. fetch('./Mine.wasm') resolves without 404. console.log prints byte length > 0.",
      "model": "sonnet"
    },
    {
      "id": "S120",
      "title": "CreateMineModal \u2014 deploy Mine contract via wallet",
      "description": "Implement handleDeploy in CreateMineModal.tsx. Use useWallet() to get senderAddress, address (walletAddress), and isConnected. Import TransactionFactory, OPNetLimitedProvider, Wallet from '@btc-vision/transaction'; BinaryWriter, Address from '@btc-vision/transaction'; networks from '@btc-vision/bitcoin'. Steps: (1) Load Mine.wasm bytecode via fetch. (2) Resolve underlying token pubkey: const code = await (provider as any).getCode(underlyingAddress); extract contractPublicKey as '0x...' hex. (3) Build calldata with BinaryWriter: writeAddress(Address.fromString(underlyingPubkey)), writeU8(18), writeStringWithLength(name), writeStringWithLength(symbol), writeU256(BigInt(wrapFee)), writeU256(BigInt(unwrapFee)), writeU256(BigInt(wrapFee)), writeU256(BigInt(unwrapFee)). (4) Use TransactionFactory.signDeployment with signer:null, mldsaSigner:null, network:NETWORK, utxos (fetch via OPNetLimitedProvider.fetchUTXOMultiAddr for walletAddress), challenge from rpc.getChallenge(). (5) Broadcast fundingTransaction then interactionTransaction via OPNetLimitedProvider. (6) Store deployed contractAddress and contractPubKey in state, show toast. Run: cd frontend && npm run build.",
      "priority": 31,
      "passes": true,
      "acceptance": "npm run build exits 0. Clicking Deploy Mine with valid inputs broadcasts two transactions. Toast shows contract address on success.",
      "model": "sonnet"
    },
    {
      "id": "S121",
      "title": "CreateMineModal \u2014 register deployed Mine in Factory",
      "description": "After successful Mine deployment (contractAddress and contractPubKey available), automatically call Factory.registerMine. Use the existing register-mine pattern from frontend: (1) Get Factory pubkey via (provider as any).getCode(CONTRACT_ADDRESSES.factory) \u2192 contractPublicKey. (2) Build calldata: BinaryWriter with writeSelector(sha256('registerMine').first4bytes), writeAddress(Address.fromString(underlyingPubkey)), writeAddress(Address.fromString(deployedMinePubkey)). (3) Fetch fresh UTXOs. (4) Get new challenge. (5) Use TransactionFactory.signInteraction with to:CONTRACT_ADDRESSES.factory, from:walletAddress, contract:factoryPubkeyHex, calldata, signer:null, mldsaSigner:null. (6) Broadcast. (7) Toast success with register tx hash. (8) Call a prop onMineCreated() so WrapPage refreshes the mine list. Run: cd frontend && npm run build.",
      "priority": 31,
      "passes": true,
      "acceptance": "npm run build exits 0. After deploy tx broadcasts, register tx is also broadcast. onMineCreated() callback triggers useMines() refetch. New mine appears in list after block confirmation.",
      "model": "sonnet"
    },
    {
      "id": "S122",
      "title": "Git commit before redeploy",
      "description": "Run git add -A && git commit -m 'before redeploy: working state checkpoint' from /home/dev/Documents/hashZroRepos/team-projects/optnet/helix root. Update progress.txt adding a line: '2026-03-05 [S122] Saved working state as git commit before redeploy. Can revert to this point if anything breaks.'",
      "priority": 122,
      "passes": true,
      "acceptance": "git log shows commit message containing 'before redeploy'. progress.txt has the checkpoint line.",
      "model": "sonnet"
    },
    {
      "id": "S123",
      "title": "Rename Mine directory and files to Genome",
      "description": "Create contracts/src/genome/ directory. Copy contracts/src/mine/Mine.ts to contracts/src/genome/Genome.ts. Copy contracts/src/mine/index.ts to contracts/src/genome/index.ts and update it to export { Genome } from './Genome'. Update contracts/asconfig.json: rename target key 'mine' to 'genome', change outFile to 'build/Genome.wasm', textFile to 'build/Genome.wat', use abort=src/genome/index/abort. Do NOT delete src/mine/ yet (keep for reference). Run cd frontend && npm run build to verify no breakage.",
      "priority": 123,
      "passes": true,
      "acceptance": "contracts/src/genome/Genome.ts exists. contracts/src/genome/index.ts exports Genome. asconfig.json has genome target with correct outFile. npm run build exits 0.",
      "model": "sonnet"
    },
    {
      "id": "S124",
      "title": "Rename class Mine to Genome in Genome.ts",
      "description": "In contracts/src/genome/Genome.ts: rename class Mine to Genome (mark @final export class Genome extends OP20). Update all internal comments from 'mine'/'Mine' to 'genome'/'Genome'. Update error message strings: 'not owner' stays, any others referencing 'mine' updated to 'genome'. No logic changes. Keep all method implementations identical.",
      "priority": 124,
      "passes": true,
      "acceptance": "contracts/src/genome/Genome.ts has 'export class Genome extends OP20'. No compilation errors when running asc on it (test compile). All method signatures unchanged.",
      "model": "sonnet"
    },
    {
      "id": "S125",
      "title": "Rename Factory.ts methods and storage from mine to genome",
      "description": "In contracts/src/factory/Factory.ts: rename methods registerMine->registerGenome, getMineCount->getGenomeCount, getMineAtIndex->getGenomeAtIndex, getMineAddress->getGenomeAddress. Rename storage pointer variables: pMineByUnderlying->pGenomeByUnderlying, pUnderlyingByMine->pUnderlyingByGenome, pMineByIndex->pGenomeByIndex, _mineCount->_genomeCount. Update all internal variable names that reference 'mine' to 'genome' (e.g. const mineAddress -> const genomeAddress). Update comments.",
      "priority": 125,
      "passes": true,
      "acceptance": "Factory.ts has methods getGenomeCount, getGenomeAtIndex, getGenomeAddress, registerGenome. Storage variable names use genome prefix. No mine references remain in method names or storage variables.",
      "model": "sonnet"
    },
    {
      "id": "S126",
      "title": "Update Factory.ts comments and error strings to genome",
      "description": "In contracts/src/factory/Factory.ts: update all comment strings from 'mine'/'Mine' to 'genome'/'Genome'. Update any error message strings (Revert messages) that mention 'mine' to use 'genome'. Change 'Mine Protocol' references to 'Genome Protocol' in comments. No logic changes.",
      "priority": 126,
      "passes": true,
      "acceptance": "Factory.ts has no remaining 'mine' or 'Mine' strings in comments or Revert messages. All replaced with genome/Genome equivalents.",
      "model": "sonnet"
    },
    {
      "id": "S127",
      "title": "Add injectRewards method to Genome.ts",
      "description": "In contracts/src/genome/Genome.ts, add a new public method after notifyAmmFee: @method() @returns({ name: 'amount', type: ABIDataTypes.UINT256 }) public injectRewards(_calldata: Calldata): BytesWriter. Implementation: (1) this.requireOwner(). (2) const amount: u256 = _calldata.readU256(). (3) if (amount == ZERO) throw new Revert('zero amount'). (4) const heldKey = this.fieldKeySimple(this._underlyingHeld). (5) this.su(heldKey, SafeMath.add(this.lu(heldKey), amount)). (6) const underlying: Address = this.la(this.fieldKeySimple(this._underlying)). (7) TransferHelper.transferFrom(underlying, Blockchain.tx.sender, Blockchain.contractAddress, amount). (8) const response = new BytesWriter(32); response.writeU256(amount); return response.",
      "priority": 127,
      "passes": true,
      "acceptance": "Genome.ts has public injectRewards method with requireOwner, zero check, increments _underlyingHeld, calls TransferHelper.transferFrom. Method is placed after notifyAmmFee.",
      "model": "sonnet"
    },
    {
      "id": "S128",
      "title": "Compile Genome contract to WASM",
      "description": "Run the genome contract compilation. First verify contracts/src/genome/index.ts correctly exports the Genome class. Then run: cd /home/dev/Documents/hashZroRepos/team-projects/optnet/helix/contracts && npx asc src/genome/index.ts --config asconfig.json --target genome. If compilation succeeds, build/Genome.wasm should exist. If there are errors, fix them in Genome.ts and re-run. Common fixes: ensure ABIDataTypes decorators are correct, SafeMath imports present, TransferHelper imported.",
      "priority": 128,
      "passes": true,
      "acceptance": "contracts/build/Genome.wasm file exists after compilation. No asc compilation errors. File size > 0 bytes.",
      "model": "sonnet"
    },
    {
      "id": "S129",
      "title": "Compile Factory contract to WASM",
      "description": "Run the Factory contract compilation: cd /home/dev/Documents/hashZroRepos/team-projects/optnet/helix/contracts && npx asc src/factory/index.ts --config asconfig.json --target factory. If compilation fails, check contracts/src/factory/Factory.ts for syntax errors from the rename in S125. Fix and re-run. Verify build/Factory.wasm exists and is non-zero.",
      "priority": 129,
      "passes": true,
      "acceptance": "contracts/build/Factory.wasm exists and is non-zero. asc compilation exits 0 with no errors.",
      "model": "sonnet"
    },
    {
      "id": "S130",
      "title": "Create deploy-genome-factory.ts deploy script",
      "description": "Create contracts/scripts/deploy-genome-factory.ts. Model after existing deploy scripts (e.g. deploy-crystal.ts or similar). The script should: (1) Load contracts/build/Factory.wasm as Uint8Array. (2) Use EC_PRIVATE_KEY + MLDSA_PRIVATE_KEY from .env. (3) Create wallet keypair. (4) Use TransactionFactory.signDeployment with the Factory.wasm bytecode and empty calldata (Factory constructor takes no params). (5) Broadcast funding + deployment transactions. (6) Print deployed bech32 address and contractPublicKey hex. Look at existing deploy scripts in contracts/scripts/ for the exact pattern.",
      "priority": 130,
      "passes": true,
      "acceptance": "contracts/scripts/deploy-genome-factory.ts exists. Script compiles (npx ts-node --version check). Script follows same pattern as existing deploy scripts.",
      "model": "sonnet"
    },
    {
      "id": "S131",
      "title": "Deploy new Factory and update config.ts",
      "description": "Run: cd /home/dev/Documents/hashZroRepos/team-projects/optnet/helix/contracts && npx ts-node scripts/deploy-genome-factory.ts. Wait for output showing new Factory bech32 address and pubkey hex. Update frontend/src/config.ts: set CONTRACT_ADDRESSES.factory to the new bech32 address, CONTRACT_ADDRESSES.factoryPubkey to the new hex pubkey. Also clean up config.ts: remove or comment out stale entries (xMotoMine, xMotoMinePubkey, old mine/staking references that no longer exist). Add comment: // Factory v3 \u2014 Genome Protocol, deployed 2026-03-05.",
      "priority": 131,
      "passes": true,
      "acceptance": "frontend/src/config.ts has new factory address and pubkey. cd frontend && npm run build exits 0. New factory responds to getGenomeCount() call.",
      "model": "sonnet"
    },
    {
      "id": "S132",
      "title": "Copy Genome.wasm to frontend public directory",
      "description": "Copy contracts/build/Genome.wasm to frontend/public/Genome.wasm. Verify the file is present. Update CreateGenomePage.tsx (or any file that fetches the WASM) to use '/Genome.wasm' instead of '/Mine.wasm'. Verify the fetch URL in CreateGenomePage references Genome.wasm.",
      "priority": 132,
      "passes": true,
      "acceptance": "frontend/public/Genome.wasm exists. CreateGenomePage.tsx fetches '/Genome.wasm'. cd frontend && npm run build exits 0.",
      "model": "sonnet"
    },
    {
      "id": "S133",
      "title": "Update MINE_ABI in contracts.ts to add injectRewards",
      "description": "In frontend/src/lib/contracts.ts, add injectRewards to MINE_ABI: { name: 'injectRewards', inputs: [], outputs: [{ name: 'amount', type: ABIDataTypes.UINT256 }], type: BitcoinAbiTypes.Function }. Also export GENOME_ABI as an alias: export const GENOME_ABI = MINE_ABI. This allows gradual migration \u2014 both names work.",
      "priority": 133,
      "passes": true,
      "acceptance": "MINE_ABI in contracts.ts includes injectRewards entry. GENOME_ABI is exported as alias. cd frontend && npx tsc --noEmit exits 0.",
      "model": "sonnet"
    },
    {
      "id": "S134",
      "title": "Update FACTORY_ABI in contracts.ts to genome method names",
      "description": "In frontend/src/lib/contracts.ts, update FACTORY_ABI method names: getMineCount->getGenomeCount, getMineAtIndex->getGenomeAtIndex, getMineAddress->getGenomeAddress, registerMine->registerGenome. These must match the newly compiled Factory.wasm method selectors exactly. Also update any comments referencing 'mine' in contracts.ts.",
      "priority": 134,
      "passes": true,
      "acceptance": "FACTORY_ABI has getGenomeCount, getGenomeAtIndex, getGenomeAddress, registerGenome. No getMineCount/getMineAtIndex/getMineAddress/registerMine remain in FACTORY_ABI. npx tsc --noEmit exits 0.",
      "model": "sonnet"
    },
    {
      "id": "S135",
      "title": "Update useMines.ts to use genome method names",
      "description": "In frontend/src/hooks/useMines.ts: update all contract method call strings from getMineCount->getGenomeCount, getMineAtIndex->getGenomeAtIndex. Rename buildGetMineAtIndexCalldata->buildGetGenomeAtIndexCalldata. Update console.log labels from '[useMines]' to '[useGenomes]'. Update internal variable names: mineAddress->genomeAddress, fetchMineData->fetchGenomeData where they appear. Keep MineInfo type name for backward compatibility (or rename to GenomeInfo and update all refs).",
      "priority": 135,
      "passes": true,
      "acceptance": "useMines.ts calls getGenomeCount and getGenomeAtIndex. buildGetGenomeAtIndexCalldata function exists. console logs say [useGenomes]. cd frontend && npx tsc --noEmit exits 0.",
      "model": "sonnet"
    },
    {
      "id": "S136",
      "title": "Update useMine.ts comments and variable names",
      "description": "In frontend/src/hooks/useMine.ts: update internal comments from mine->genome. Update variable names: mineContract->genomeContract, mineCode->genomeCode, mineAddress->genomeAddress in the hook body (but keep the exported function name useMine for now). Update console.log labels. No logic changes.",
      "priority": 136,
      "passes": true,
      "acceptance": "useMine.ts has genomeContract, genomeCode variable names internally. Comments use genome terminology. npx tsc --noEmit exits 0.",
      "model": "sonnet"
    },
    {
      "id": "S137",
      "title": "Update CreateGenomePage.tsx WASM path and registerGenome selector",
      "description": "In frontend/src/pages/CreateGenomePage.tsx: (1) Change fetch URL from '/Mine.wasm' to '/Genome.wasm'. (2) Update the registerMine selector computation to registerGenome: change createHash('sha256').update('registerMine()') to createHash('sha256').update('registerGenome()'). (3) Update any other hardcoded 'registerMine' or 'Mine.wasm' strings. (4) Run cd frontend && npm run build to verify.",
      "priority": 137,
      "passes": true,
      "acceptance": "CreateGenomePage.tsx fetches /Genome.wasm. Selector uses 'registerGenome()'. npm run build exits 0.",
      "model": "sonnet"
    },
    {
      "id": "S138",
      "title": "Make MotoSwap pool creation mandatory in CreateGenomePage.tsx",
      "description": "In frontend/src/pages/CreateGenomePage.tsx, after successful Factory registerGenome call (step 2), add mandatory step 3: create MotoSwap pool. Import MotoSwapFactoryAbi, IMotoswapFactoryContract from 'opnet'. Import Address from '@btc-vision/transaction'. Use CONTRACT_ADDRESSES.motoswapFactory. Call: const motoFactory = getContract<IMotoswapFactoryContract>(Address.fromString(CONTRACT_ADDRESSES.motoswapFactory), MotoSwapFactoryAbi as any, provider, NETWORK, senderAddress); const poolSim = await motoFactory.createPool(Address.fromString(deployedGenomePubkey), Address.fromString(underlyingPubkey)); await poolSim.sendTransaction({...omit signer/mldsaSigner...}). Show 'Creating liquidity pool...' in progress. Show pool address on success. If step fails, show error but do not block (genome is already registered).",
      "priority": 138,
      "passes": false,
      "acceptance": "CreateGenomePage shows step 3 'Creating Pool' after registration. On success shows pool address. MotoSwap factory createPool is called with gToken and underlying pubkeys. npm run build exits 0.",
      "model": "sonnet"
    },
    {
      "id": "S139",
      "title": "Enforce gToken symbol prefix 'g' in CreateGenomePage",
      "description": "In frontend/src/pages/CreateGenomePage.tsx: (1) Add validation on the symbol field: if value doesn't start with lowercase 'g', show inline error message 'Must start with g (e.g. gMOTO)' in red below the field. (2) Disable the Create/Deploy button when symbol is empty or does not start with 'g'. (3) Add state for symbolError. On symbol change: setSymbolError(val.length > 0 && !val.startsWith('g') ? 'Must start with g (e.g. gMOTO)' : '').",
      "priority": 139,
      "passes": false,
      "acceptance": "Entering 'MOTO' in symbol field shows red error 'Must start with g'. Deploy button is disabled. Entering 'gMOTO' clears error and enables button. npm run build exits 0.",
      "model": "sonnet"
    },
    {
      "id": "S140",
      "title": "Add live gToken symbol preview in CreateGenomePage",
      "description": "In frontend/src/pages/CreateGenomePage.tsx: (1) Below the symbol input field, add a preview section shown when symbol starts with 'g'. Style: border: '1px solid #000', padding: '8px 12px', fontFamily: 'Sometype Mono', showing 'Preview: {symbol}'. (2) Above the symbol field, add a mandatory note: '* gToken symbols must begin with g (e.g. gMOTO, gPILL)' in small grey Sometype Mono text. (3) The preview updates live as user types.",
      "priority": 140,
      "passes": false,
      "acceptance": "Typing 'gMOTO' in symbol field shows 'Preview: gMOTO' box below. Mandatory note is visible above field. npm run build exits 0.",
      "model": "sonnet"
    },
    {
      "id": "S141",
      "title": "Auto-prepend g prefix in CreateGenomePage symbol field",
      "description": "In frontend/src/pages/CreateGenomePage.tsx symbol input: on onBlur, if the field value is non-empty and doesn't start with 'g', auto-prepend 'g': setSymbol('g' + value). Also clear the error after auto-fix. This way users who type 'MOTO' have it auto-corrected to 'gMOTO' when they tab away. Do not auto-prepend while user is actively typing (only onBlur).",
      "priority": 141,
      "passes": false,
      "acceptance": "Typing 'MOTO' and tabbing away auto-corrects to 'gMOTO'. Error clears. npm run build exits 0.",
      "model": "sonnet"
    },
    {
      "id": "S142",
      "title": "Update CreateGenomePage copy from xToken to gToken",
      "description": "In frontend/src/pages/CreateGenomePage.tsx: replace all user-facing display strings 'xToken'->'{symbol} (gToken)', 'x-Token'->'gToken', 'Mine'->'Genome', 'mine'->'genome' in labels, headings, helper text, step descriptions, button text. Do not change variable names, only string literals displayed to user. Also update page <title> or heading if it says 'Create Mine'.",
      "priority": 142,
      "passes": false,
      "acceptance": "No 'xToken' or 'Mine' visible in the CreateGenomePage UI. All replaced with gToken/Genome. npm run build exits 0.",
      "model": "sonnet"
    },
    {
      "id": "S143",
      "title": "Replace MineCard grid in MyGenomesPage with GenomeOwnerCard",
      "description": "In frontend/src/pages/MyGenomesPage.tsx: create a GenomeOwnerCard component (in the same file) that replaces MineCard for the owner view. GenomeOwnerCard props: mine: MineInfo. Shows: genome name, symbol (with g prefix styling), ratio (6 decimal places), underlyingBalance formatted, underlyingSymbol, and a status area for pool info (to be filled in S144-S145). Add placeholder 'Loading pool...' text where pool info will go. Replace MineCard with GenomeOwnerCard in the grid.",
      "priority": 143,
      "passes": false,
      "acceptance": "MyGenomesPage uses GenomeOwnerCard instead of MineCard. Each card shows name, symbol, ratio, underlyingBalance. npm run build exits 0.",
      "model": "sonnet"
    },
    {
      "id": "S144",
      "title": "Add useGenomePoolInfo hook to fetch pool status",
      "description": "Create frontend/src/hooks/useGenomePoolInfo.ts. Hook signature: useGenomePoolInfo(genomeAddress: string | null, genomePubkey: string, underlyingPubkey: string, senderAddress: Address | null). Implementation: (1) Get pool address via MotoSwapFactory.getPool(genomePubkey, underlyingPubkey) \u2014 use provider (no sender needed for reads). (2) If pool address is non-zero, get reserves via MotoswapPoolAbi getReserves() call on pool address. (3) Get user LP balance via OP_20_ABI balanceOf(senderAddress) on pool address. Returns: { poolAddress: string, reserve0: bigint, reserve1: bigint, lpBalance: bigint, loading: boolean, error: string | null }. Import MotoSwapFactoryAbi, MotoswapPoolAbi from 'opnet'. Import CONTRACT_ADDRESSES from config.",
      "priority": 144,
      "passes": false,
      "acceptance": "frontend/src/hooks/useGenomePoolInfo.ts exists and exports useGenomePoolInfo. Hook returns poolAddress, reserve0, reserve1, lpBalance. npx tsc --noEmit exits 0.",
      "model": "sonnet"
    },
    {
      "id": "S145",
      "title": "Display pool status panel in GenomeOwnerCard",
      "description": "In frontend/src/pages/MyGenomesPage.tsx GenomeOwnerCard: use useGenomePoolInfo hook to fetch pool data. Display pool panel below genome stats: (1) If loading: show skeleton. (2) If poolAddress is zero/empty: show 'No Pool' badge in grey border. (3) If pool exists: show pool address truncated (first 8 + last 6 chars) with copy button, reserve0 formatted, reserve1 formatted, user LP balance formatted. Section label 'Liquidity Pool' in Sometype Mono uppercase grey.",
      "priority": 145,
      "passes": false,
      "acceptance": "GenomeOwnerCard shows pool address, reserves, LP balance when pool exists. Shows 'No Pool' when pool address is zero. npm run build exits 0.",
      "model": "sonnet"
    },
    {
      "id": "S146",
      "title": "Add Inject Rewards button to GenomeOwnerCard",
      "description": "In frontend/src/pages/MyGenomesPage.tsx GenomeOwnerCard: add 'Inject Rewards' action section. Show: (1) Amount input (text field) for underlying token amount. (2) Button 'Inject Rewards' styled like ActionBtn (black border, inverts on hover). On click: (a) Parse amount. (b) Import OP_20_ABI, GENOME_ABI from contracts.ts. (c) Call underlyingContract.increaseAllowance(Address.fromString(mine.pubkey), parsedAmount) and send. (d) Call genomeContract.injectRewards(parsedAmount) and send. (e) Show success toast 'Rewards injected \u2014 ratio increased'. Handle errors. Only shown when isConnected.",
      "priority": 146,
      "passes": false,
      "acceptance": "GenomeOwnerCard has amount input and Inject Rewards button. Clicking it approves underlying then calls injectRewards. Toast shows on success. npm run build exits 0.",
      "model": "sonnet"
    },
    {
      "id": "S147",
      "title": "Add Claim LP Rewards button to GenomeOwnerCard",
      "description": "In frontend/src/pages/MyGenomesPage.tsx GenomeOwnerCard: add 'Claim LP Rewards' button. Show only when lpBalance > 0n. Button label: 'Claim LP Fees -> Genome'. On click: set a claiming state to show progress steps. Button disabled during claim. Show step indicator: 'Step 1/3: Approving LP...', 'Step 2/3: Removing liquidity...', 'Step 3/3: Injecting rewards...'. This is the container \u2014 actual logic in S148 and S149.",
      "priority": 147,
      "passes": false,
      "acceptance": "Claim LP Rewards button shows only when lpBalance > 0. Button shows step progress during claim. npm run build exits 0.",
      "model": "sonnet"
    },
    {
      "id": "S148",
      "title": "Implement Claim LP Rewards step 1 and 2 in MyGenomesPage",
      "description": "In frontend/src/pages/MyGenomesPage.tsx GenomeOwnerCard claim handler: Step 1 \u2014 approve LP tokens to router: getContract with MotoswapPoolAbi on poolAddress (with senderAddress), call increaseAllowance(Address.fromString(CONTRACT_ADDRESSES.motoswapRouter), lpBalance), sendTransaction. Step 2 \u2014 remove liquidity: getContract with MOTOSWAP_ROUTER_ABI on CONTRACT_ADDRESSES.motoswapRouter (with senderAddress), call removeLiquidity(gTokenAddress, underlyingAddress, lpBalance, 0n, 0n, walletSenderAddress, deadline=BigInt(Math.floor(Date.now()/1000)+1200)), sendTransaction. Extract underlyingAmount from result (amount of underlying token returned). Import MOTOSWAP_ROUTER_ABI, MotoswapPoolAbi from 'opnet'.",
      "priority": 148,
      "passes": false,
      "acceptance": "Claim flow steps 1 and 2 execute: LP approval tx sent, removeLiquidity tx sent. underlyingAmount extracted from result. npm run build exits 0.",
      "model": "sonnet"
    },
    {
      "id": "S149",
      "title": "Implement Claim LP Rewards step 3 \u2014 injectRewards in MyGenomesPage",
      "description": "In frontend/src/pages/MyGenomesPage.tsx claim handler after removeLiquidity: Step 3 \u2014 inject underlying into genome. (a) getContract with GENOME_ABI on mine.address (with senderAddress), call underlyingContract.increaseAllowance(Address.fromString(mine.pubkey), underlyingAmount), send. (b) call genomeContract.injectRewards(underlyingAmount), send. (c) Show success state: 'LP fees claimed and injected into genome! Ratio increased.' (d) Refetch pool info and mine data. gToken amount returned from removeLiquidity stays in user wallet (do not re-wrap automatically).",
      "priority": 149,
      "passes": false,
      "acceptance": "Step 3 executes: underlying approved to genome, injectRewards called. Success message shown. Pool info refetched. npm run build exits 0.",
      "model": "sonnet"
    },
    {
      "id": "S150",
      "title": "User-facing text pass \u2014 mine to genome across all pages",
      "description": "Scan and update all user-facing display strings across the frontend. Files to update: frontend/src/pages/HomePage.tsx, MineDetailPage.tsx, CreateGenomePage.tsx, MyGenomesPage.tsx, frontend/src/components/MineCard.tsx, Navbar.tsx. Replace in string literals only (not variable names): 'Mine'->'Genome', 'mine'->'genome', 'xToken'->'gToken', 'x-token'->'gToken', 'wrapped token'->'gToken'. Also update page headings and empty state messages. Do NOT rename variables, types, or function names.",
      "priority": 150,
      "passes": false,
      "acceptance": "No 'Mine', 'xToken' visible in any page's displayed text. All replaced with Genome/gToken. npm run build exits 0.",
      "model": "sonnet"
    },
    {
      "id": "S151",
      "title": "Add Owner Actions section to MineDetailPage",
      "description": "In frontend/src/pages/MineDetailPage.tsx: add an 'Owner Actions' section shown only when isConnected and identityKey === mine.ownerAddress (case-insensitive). Section contains: (1) Inject Rewards: amount input + button that calls increaseAllowance then injectRewards(amount). (2) Set Wrap Fee: number input (0-20 = 0%-20%) + button that calls setWrapFee. (3) Set Unwrap Fee: same for setUnwrapFee. Style consistently with existing ActionBtn. Place section after the Protocol stats table and before modals.",
      "priority": 151,
      "passes": false,
      "acceptance": "Owner Actions section appears only when wallet is connected and user is genome owner. injectRewards, setWrapFee, setUnwrapFee buttons work. npm run build exits 0.",
      "model": "sonnet"
    },
    {
      "id": "S152",
      "title": "Add Liquidity Pool section to MineDetailPage",
      "description": "In frontend/src/pages/MineDetailPage.tsx: add a 'Liquidity Pool' section. Use useGenomePoolInfo hook (from S144) with mine.address, mine.pubkey, mine.underlyingPubkey, senderAddress. Display: (1) Pool address truncated with copy button and OpScan link. (2) Reserve0 (gToken) and Reserve1 (underlying) formatted amounts. (3) If no pool: show 'No pool exists' + 'Create Pool' button that calls motoFactory.createPool(gTokenPubkey, underlyingPubkey). Place section between Protocol stats and Owner Actions.",
      "priority": 152,
      "passes": false,
      "acceptance": "MineDetailPage shows Liquidity Pool section with pool address, reserves. Create Pool button appears when no pool. npm run build exits 0.",
      "model": "sonnet"
    },
    {
      "id": "S153",
      "title": "Add gToken badge to MineCard",
      "description": "In frontend/src/components/MineCard.tsx: next to the token symbol in the header, add a small 'g' badge \u2014 a span with border: '1px solid currentColor', padding: '1px 4px', fontSize: '0.6rem', fontFamily: 'Sometype Mono' showing 'gToken'. Color matches card state (dark on white, light on black hover). Position it after the symbol text in the top row. Only show if symbol starts with 'g'.",
      "priority": 153,
      "passes": false,
      "acceptance": "MineCard shows 'gToken' badge next to symbol when symbol starts with 'g'. Badge inverts color on hover. npm run build exits 0.",
      "model": "sonnet"
    },
    {
      "id": "S154",
      "title": "Navbar cleanup \u2014 verify My Genomes tab and remove stale code",
      "description": "In frontend/src/components/Navbar.tsx: verify the My Genomes NavTab links to /my-genomes and shows correctly when connected. Remove any stale imports or variables (there should be no useMines import, no myGenomeCount). Verify useWallet returns isConnected correctly. Run npm run build and fix any TypeScript errors.",
      "priority": 154,
      "passes": false,
      "acceptance": "Navbar.tsx has no stale imports. My Genomes tab shows for connected users. npm run build exits 0.",
      "model": "sonnet"
    },
    {
      "id": "S155",
      "title": "Clean up config.ts and add genome version constant",
      "description": "In frontend/src/config.ts: (1) Remove or comment out stale addresses: xMotoMine, xMotoMinePubkey, mine, minePubkey, staking, stakingPubkey (these refer to old deployed contracts). (2) Add export const GENOME_CONTRACT_VERSION = 2. (3) Add comment block at top: '// Factory v3 \u2014 Genome Protocol deployed 2026-03-05'. (4) Keep motoswapRouter, motoswapFactory, motoToken, pillToken and other token addresses. (5) Update HIDDEN_MINE_PUBKEYS comment to HIDDEN_GENOME_PUBKEYS.",
      "priority": 155,
      "passes": false,
      "acceptance": "config.ts has no stale mine/staking addresses. GENOME_CONTRACT_VERSION exported. npm run build exits 0.",
      "model": "sonnet"
    },
    {
      "id": "S156",
      "title": "Update AGENTS.md with Genome contract and LP claim patterns",
      "description": "Append to /home/dev/Documents/hashZroRepos/team-projects/optnet/helix/AGENTS.md: (1) Genome contract patterns: injectRewards method signature, gToken naming rules (must start with g), Genome class extends OP20. (2) MotoSwap pool interaction pattern: useGenomePoolInfo hook usage, getPool/createPool/getReserves call patterns. (3) LP Claim flow: 5-step sequence (getPool, getLPBalance, increaseAllowance LP->router, removeLiquidity, injectRewards). (4) Address pattern for contract addresses: Address.fromString(hex) with one arg.",
      "priority": 156,
      "passes": false,
      "acceptance": "AGENTS.md has new sections for Genome contract, MotoSwap pool interaction, LP claim flow. File is valid markdown.",
      "model": "sonnet"
    },
    {
      "id": "S157",
      "title": "Update progress.txt with Phase 8-12 summary",
      "description": "Update /home/dev/Documents/hashZroRepos/team-projects/optnet/helix/progress.txt: add Phase 8 (Contract Refactor), Phase 9 (Frontend Hooks), Phase 10 (gToken Enforcement), Phase 11 (My Genomes LP Claim), Phase 12 (Branding) as completed phases. Note: new Factory address, Genome.wasm compiled, injectRewards added, LP claim flow implemented. Format consistently with existing progress.txt entries.",
      "priority": 157,
      "passes": false,
      "acceptance": "progress.txt has Phase 8-12 summary. Notes new factory address and key features added.",
      "model": "sonnet"
    },
    {
      "id": "S158",
      "title": "TypeScript build verification \u2014 fix all errors",
      "description": "Run: npx tsc --noEmit 2>&1. Read the output and fix ALL TypeScript errors found. Common issues to look for: missing imports for useGenomePoolInfo, type mismatches in GenomeOwnerCard, Address usage patterns. After fixing, run npm run build to confirm clean production build.",
      "priority": 158,
      "passes": false,
      "acceptance": "npx tsc --noEmit exits 0 with no errors. npm run build exits 0. No type errors remain.",
      "model": "sonnet"
    },
    {
      "id": "S159",
      "title": "Create register-genome-test.ts script",
      "description": "Create contracts/scripts/register-genome-test.ts. This script registers a test genome (gMOTO wrapping motoToken) with the new Factory. Use the registerGenome() selector (SHA256('registerGenome()').first4bytes). Read FACTORY_ADDRESS (new from config), MOTO_UNDERLYING (motoToken pubkey) from .env or hardcode. Modeled after existing register scripts but uses 'registerGenome()' as the method string in selector computation. Print the result of getGenomeCount() after registration to verify.",
      "priority": 159,
      "passes": false,
      "acceptance": "contracts/scripts/register-genome-test.ts exists and compiles. Script uses registerGenome() selector. Script structure matches existing register scripts.",
      "model": "sonnet"
    },
    {
      "id": "S160",
      "title": "Register gMOTO test genome with new Factory",
      "description": "Run: cd /home/dev/Documents/hashZroRepos/team-projects/optnet/helix/contracts && npx ts-node scripts/register-genome-test.ts. Wait for confirmation. Then verify the frontend shows the genome: open the dev server, check the Explore page, genome count in stats should be >= 1. Also run check-factory.ts or similar to verify getGenomeCount() returns > 0 from the new factory.",
      "priority": 160,
      "passes": false,
      "acceptance": "register-genome-test.ts runs successfully. Frontend Explore page shows at least 1 genome from the new factory. getGenomeCount() > 0.",
      "model": "sonnet"
    },
    {
      "id": "S161",
      "title": "Verify createPool auto-creation during genome creation",
      "description": "Using the frontend dev server, create a new test genome (any token as underlying, symbol starting with g). Verify that after registration Step 3 runs (Creating Pool...) and shows a pool address. Then verify: call motoFactory.getPool(gTokenPubkey, underlyingPubkey) in a check script or via the frontend and confirm it returns a non-zero address. Document the pool address in progress.txt.",
      "priority": 161,
      "passes": false,
      "acceptance": "Creating a genome via frontend UI results in pool creation step completing. Pool address is non-zero. MotoSwap factory getPool returns it.",
      "model": "sonnet"
    },
    {
      "id": "S162",
      "title": "Verify injectRewards increases ratio",
      "description": "Using My Genomes page on the frontend: select a genome you own that has some gTokens minted. Enter an amount of underlying tokens to inject (you must have them in your wallet). Click Inject Rewards. Verify: (1) Transaction broadcasts successfully. (2) Page refetches and shows updated ratio (should be > previous ratio). (3) underlyingBalance increased. Document the ratio change in progress.txt.",
      "priority": 162,
      "passes": false,
      "acceptance": "injectRewards transaction succeeds on testnet. Genome ratio increases after injection. progress.txt notes the verification.",
      "model": "sonnet"
    },
    {
      "id": "S163",
      "title": "Verify end-to-end LP claim flow",
      "description": "End-to-end test of LP claim: (1) Add liquidity to a genome's pool via MotoSwap or the frontend. (2) Verify LP balance shows > 0 in My Genomes page GenomeOwnerCard. (3) Click Claim LP Fees -> Genome. (4) Complete all 3 steps (approve LP, removeLiquidity, injectRewards). (5) Verify genome ratio increased. Document result in progress.txt.",
      "priority": 163,
      "passes": false,
      "acceptance": "Full LP claim flow completes on testnet. LP balance decreases to 0. Genome ratio increases. progress.txt notes verification.",
      "model": "sonnet"
    },
    {
      "id": "S164",
      "title": "Update SUBMISSION.md with new features",
      "description": "Update /home/dev/Documents/hashZroRepos/team-projects/optnet/helix/SUBMISSION.md: add section 'New Features (Phase 2)' documenting: (1) gToken naming standard \u2014 all genome tokens start with g prefix, enforced in UI. (2) injectRewards \u2014 genome owners can boost yield by injecting underlying tokens. (3) Automatic MotoSwap pool creation on genome deploy. (4) LP Claim Flow \u2014 My Genomes page lets owners remove LP fees and inject them into genome. (5) New Factory address. Keep existing content.",
      "priority": 164,
      "passes": false,
      "acceptance": "SUBMISSION.md has Phase 2 features section with all 4 new features documented. File is valid markdown.",
      "model": "sonnet"
    },
    {
      "id": "S165",
      "title": "Update prd.json \u2014 mark S122-S164 complete and update metadata",
      "description": "Update /home/dev/Documents/hashZroRepos/team-projects/optnet/helix/prd.json: (1) For each story S122-S164 that has been successfully implemented, set passes: true. (2) Update project description to include 'Genome Protocol \u2014 gToken yield wrapping'. Verify all stories have acceptance criteria fields.",
      "priority": 165,
      "passes": false,
      "acceptance": "prd.json stories S122-S164 have passes: true. Project description updated. JSON is valid.",
      "model": "sonnet"
    },
    {
      "id": "S166",
      "title": "Final feature commit for genome protocol v2",
      "description": "Run git add -A from /home/dev/Documents/hashZroRepos/team-projects/optnet/helix. Then commit with message: 'feat: genome protocol v2 \u2014 gToken naming, injectRewards, LP claim flow'. Stage all relevant files: contracts/src/genome/, contracts/scripts/deploy-genome-factory.ts, contracts/scripts/register-genome-test.ts, frontend/src/, AGENTS.md, SUBMISSION.md, progress.txt, prd.json.",
      "priority": 166,
      "passes": false,
      "acceptance": "git log shows commit 'feat: genome protocol v2'. All changed files included in commit.",
      "model": "sonnet"
    },
    {
      "id": "S167",
      "title": "Production build verification",
      "description": "Run: cd /home/dev/Documents/hashZroRepos/team-projects/optnet/helix/frontend && npm run build. Verify dist/ directory is generated. Check that Genome.wasm is included in dist/. Verify bundle size is reasonable. If build fails, fix errors and re-run.",
      "priority": 167,
      "passes": false,
      "acceptance": "npm run build exits 0. dist/ directory exists with index.html, assets/, Genome.wasm. No build errors.",
      "model": "sonnet"
    },
    {
      "id": "S168",
      "title": "Vercel or IPFS deployment",
      "description": "Deploy the frontend. If using Vercel: run vercel --prod from frontend/. If using IPFS: use the ipfs deploy command or service configured. Verify the deployed URL loads correctly. Test navigation: /, /mine/:address, /create, /my-genomes all load. Verify wallet connection works on deployed version.",
      "priority": 168,
      "passes": false,
      "acceptance": "Frontend is deployed and accessible at a public URL. All routes load. Wallet connection functions.",
      "model": "sonnet"
    },
    {
      "id": "S169",
      "title": "Update README.md with Genome Protocol description",
      "description": "Update /home/dev/Documents/hashZroRepos/team-projects/optnet/helix/README.md: (1) Change project name to 'Helix \u2014 Genome Protocol'. (2) Update description to explain gToken standard: wrap any OP-20 token, get gTokens that appreciate via fee yield. (3) Explain injectRewards. (4) Explain LP claim flow. (5) Update any references to 'mine' or 'xToken' to genome/gToken. Keep technical setup instructions.",
      "priority": 169,
      "passes": false,
      "acceptance": "README.md references Genome Protocol, gToken, injectRewards, LP claim. No mine/xToken references in user-facing description. File is valid markdown.",
      "model": "sonnet"
    },
    {
      "id": "S170",
      "title": "Update prd.json project metadata to Genome Protocol",
      "description": "Update /home/dev/Documents/hashZroRepos/team-projects/optnet/helix/prd.json: change project field to 'Genome Protocol \u2014 OPNet Port'. Update description field to: 'Token-wrapping yield protocol on Bitcoin L1. Wrap OP-20 tokens into gTokens (yield-bearing genome tokens). Genome owners earn yield via wrap/unwrap fees. LP rewards can be injected into genomes to increase the gToken ratio.' Save and verify JSON is valid.",
      "priority": 170,
      "passes": false,
      "acceptance": "prd.json project field is 'Genome Protocol \u2014 OPNet Port'. Description reflects gToken/genome terminology. JSON is valid (python3 -c 'import json; json.load(open(\"prd.json\"))' exits 0).",
      "model": "sonnet"
    },
    {
      "id": "S171",
      "title": "Ralph completion checkpoint \u2014 verify all 50 stories pass",
      "description": "Verify all stories S122-S171 have passes: true in prd.json. Run: cd /home/dev/Documents/hashZroRepos/team-projects/optnet/helix/frontend && npm run build one final time. Update progress.txt with final status: 'Phase 8-13 complete. 171/171 stories passing. Genome Protocol v2 deployed.' Commit progress.txt update.",
      "priority": 171,
      "passes": false,
      "acceptance": "prd.json shows passes: true for S122-S171. npm run build exits 0. progress.txt notes 171/171 stories complete.",
      "model": "sonnet"
    }
  ]
}